---
context_role: contract
read_policy: on-demand
---

# Temporary Content Governance Contract

## Classification Rule

A path name never determines authority, ownership or deletion. For every temporary, generated, recovery, benchmark, release or work-product class, classify the writer/reader, authority, location, tracked/ignored state, sensitivity/disclosure boundary, retention/TTL/capacity, atomicity/concurrency/collision/path-link behavior, cleanup owner/failure behavior, Windows/macOS semantics, recovery role and final-Source eligibility. Before deleting or migrating a class, search its current writer, reader, migration, tests, public documentation and tracked/user-owned content.

Only the creating owner removes content it can positively identify as its own. Missing, stale or corrupt temporary material may increase rework, but cannot create a passing or completed result. Cleanup failure is explicit; deletion never follows a symlink/junction or crosses the repository/declared temporary root.

## Current Classes

| Class / location | Owner and authority | Tracking, sensitivity and retention | Atomicity, collision and cleanup | Recovery and final-Source boundary |
| --- | --- | --- | --- | --- |
| Tracked `.work_products/**/delivery-contract.yaml` | User/Long-Task Source and Contract owners; may be active delivery authority input | Tracked; task/delivery lifetime; may contain task semantics and follows Source disclosure | Never blanket-cleaned; changed/explicit paths remain verification inputs; exact migration readers must be preserved | May be formal Source/Contract work product; directory name does not make it cache |
| Untracked `.work_products/**` | User/Agent scratch unless a selected workflow assigns a stronger role | Untracked; unknown sensitivity; no default TTL or capacity promise | Inferred affected-test discovery may omit and report it, but no general cleanup owner exists | Not automatically Authority or final Source; explicit paths remain fail-safe inputs |
| `tmp/ty-context/context-exports/**` | `export-context`/Source Pack owner; non-authoritative export | Ignored; disclosure-sensitive derived project content; `latest/` is bounded to the current export set while custom exports are user-retained | Export owner validates containment and only replaces/removes its declared export set; failure must not affect Context | Recovery/transfer aid only; forbidden as a `project_context/context.toml` node or sole durable Source |
| `tmp/ty-context/source-preview/**` | source-preview pack tool; package-review artifact | Ignored; retained until maintainer cleanup; package-size capacity is command/report bounded | `--clean` is scoped to the explicit output; user-chosen output/collisions are protected | Install/review input, never release authority or durable project Source |
| `.artifacts/mechanism/**`, `.artifacts/mechanism-admission/**` and other local benchmark artifacts | benchmark operator; measurement evidence only | Ignored; raw run directories may include prompts, model results, traces, stderr, gold-derived scores and token/tool data; sanitized admission bundle/payload excludes those classes; retain per experiment policy | Runner creates only explicit collision-free output directories and its OS invocation temp; no product command scans/deletes other artifacts | Audit evidence, never Context/Contract/Progress/acceptance; published claims require frozen eligible pairs and exact identities |
| Remote mechanism-admission CI artifact | admission-evidence workflow; sanitized transport only | Untracked remote artifact; CI-current deterministic JSON plus digest-bound pair/aggregate summary projections, attestation and manifest; 30-day retention; no findings, prompts, raw model answers, events, stderr or sensitive Source | Local sanitizer validates complete inputs before projection; workflow accepts a size-bounded payload, revalidates exact `main` commit/tree, independently installs/builds/runs deterministic DRA and package parity, requires the current CI deterministic digest to match the materialized evidence, records distinct input-payload/materialized-JSON-set/uploaded-artifact digests, then `upload-artifact` owns retention/deletion | Reproducibility aid only; deterministic runtime, Fresh-Agent internal validity and provenance qualification remain separate and never become Context, Authority, Evidence, Receipt, Gate or delivery acceptance |
| `.artifacts/releases/prepared/**` and release attestations | release prepare/publish owner; immutable publication handoff when a release is explicitly run | Ignored artifact directory plus tracked release metadata where declared; release-run retention | Exact hash/commit attestation, no repack in publish; cleanup belongs to release operator | May authorize the named publish operation, but is not workflow acceptance or ordinary disposable cache |
| Test-suite timing/build-fingerprint artifacts | package test runner/build owner; diagnostics/fresh-build guard | Ignored/ephemeral; no product data authority; bounded to current run/build inputs | Current-run terminal records, deterministic top-10; runner owns replacement/cleanup | Diagnostic/test selection input only, never reusable acceptance or Context |
| Design-resource handoff drafts and same-parent `.tmp-*` bundle directory | DRA author/bundle command; drafts are ordinary pending Source, temp directory is command-owned | Draft tracking follows user-chosen project-native location; temp sibling is ephemeral | Output must not exist; exclusive same-volume temp and rename; only the created temp is removed, cleanup failure is explicit | Published handoff can be final Source; draft/temp sibling cannot be sole accepted handoff |
| DRA `tmp/ty-context/design-resource-recovery/<session>/checkpoint.json` and operation temp | DRA recovery helper; non-authoritative workflow recovery state | Ignored, task-local, explicitly non-sensitive/disclosure-reviewed, no global TTL/registry; delete after safe handoff or user abandonment | Strict owner marker/schema/session, checkpoint/target CAS, no-follow components, exclusive same-dir temp/rename; removal inventories the session first and completes only for helper-owned entries, otherwise returns an explicit partial result and preserves unowned content | Conditional recovery input only; raw-digest-bound marked Source references may recover authority but checkpoint bytes never become Source/Contract/Authority/Evidence/Receipt/Gate/acceptance or final-source eligible |
| Long-Task workdir `.ty-context/**` | Long-Task lifecycle owner; compiled/Progress/pending/final projections have typed recovery/audit roles | Worktree-local task lifetime; governed by Long-Task cleanup/lifecycle rather than generic temp policy | Existing identity/freshness/CAS rules; do not move or bulk-delete | Recovery/audit projections; they do not replace Source/Contract Active Authority or current Final Gate |
| Git common-dir Active Authority and worktree binding | Long-Task Authority owner | Persist for active machine authority; not ordinary temp despite generated location | Existing lock/CAS/close/abandon semantics only | Machine authority; never move into `tmp` or subject to generic cleanup |
| OS-temp workspace snapshots, Counterfactual sandboxes and disposable test repositories | Owning verification/test command | Process/command lifetime; may contain project bytes; OS temp disclosure boundary | Unique directory, scoped recursive cleanup on success/failure; failures explicit where they affect assurance; Windows handles may require fail-closed retry/report, not blind deletion | Execution isolation only, never final Source/Authority/evidence after owner result is derived |
| `.codex/hooks.json`, package profile publication temps and exact legacy paths | Package sync/Hook/profile/migration owners | Ignored runtime config or exact migration-only content; may contain user entries | no-follow parent/target, content snapshot/CAS, exclusive same-dir temp, atomic rename; preserve user collisions and remove only exact package-owned content | Host/runtime configuration, not delivery Source or acceptance; migration-only paths are not current state |
| Generic `tmp/`, `temp/`, `artifacts/`, `reports/` outside named owners | Unknown/user-owned by default | Ignored does not imply safe or insensitive; no inferred retention/TTL/capacity | No package-wide cleanup or scan; require an explicit writer/reader/ownership decision first | Never automatically final Source, but content cannot be deleted or downgraded merely by directory name |

## Platform Boundary

Repository-relative identities use `/`; filesystem operations use Node path APIs. Same-directory rename is the atomic publication boundary on supported Windows/macOS filesystems. Owners must reject link/reparse-point parents and non-regular destinations, keep temporary files on the target volume, recheck current content before replacement and reread after material write when correctness depends on exact bytes. File data synchronization and rename semantics are OS/filesystem TCB; no owner claims crash-proof storage or hostile-writer isolation beyond its tested boundary.

## Non-Goals

There is no global Temporary Artifact Manager, retention daemon, directory-name deletion rule, session registry or acceptance cache. This contract does not move Long-Task Authority, turn benchmark/release artifacts into Context, or make ignored files safe to disclose.
