---
context_role: implementation-index
read_policy: on-demand
---

# Harness Package Implementation Index

## Role

Navigation for current implementation surfaces. Behavior is defined by owning contract/area Context and `PROJECT_SPEC.md`.

- Long-Task design rationale and mechanism-to-risk map: `project_context/areas/harness-package/decision-rationale/long-task-workflow.md`.
- Shared Engineering Quality purpose, Semantic Fact boundary, Architecture Deliberation/Conformance, applicable-quality routing, implementation discipline, modularity capability and update principles retain the compatibility-stable locator `project_context/areas/harness-package/decision-rationale/architecture-quality.md`; normative order and carrier ownership live in `contracts/workflow-contract.md`.

## Workflow Assurance Boundary Owners

- Superseding Source: `docs/workflow-assurance-boundary.md`.
- Stable three-axis positioning and default/Long-Task responsibility boundary: `PROJECT_SPEC.md`, `foundation/context-model.md`, `contracts/workflow-contract.md` and `decision-rationale/long-task-workflow.md`.
- Default runtime guidance: `.codex/ty-context-managed/agents/AGENTS_CORE.md` plus the package-managed product, surface, UI/UX and engineering role Skills. `context_development_engineer/references/engineering-design-reasoning.md` and `context_uiux_design/references/task-uiux-analysis.md` own their respective on-demand material reasoning; root/generated/package copies follow the existing `packages/ty-context/source-mappings.yaml` copy-tree entries and workspace sync.
- Source-backed non-UI and selected-design boundary: `docs/non-ui-semantic-fact-completeness.md` and `docs/design-fact-completeness.md`. Formal Long-Task exact closure remains implemented by the existing semantic/design Source, Contract and evidence owners below.
- Public positioning: `README.md`, `README.zh-CN.md` and `packages/ty-context/README.md`.
- Owner-scoped portability diagnostic: `tools/verify_active_source_portability.mjs` scans only its explicit managed/package/public/Context/source-mapping/runtime-Source owners plus caller-supplied exact active Source paths. `tests/ty-context/active-source-portability.test.mjs` owns its fail-closed locator controls. Historical Contracts/Receipts/fixtures are not a hidden global registry and are never rewritten or deleted by this tool.
- Static executable continuity: `workflow-contract-routing.test.mjs` owns explicit/bound-only routing, assurance selection, default positive/negative sentinels and managed parity; `long-task-semantic-guidance.cases.mjs` separates default material-requirement guidance from Long-Task exact closure; `visual-delivery-guidance.test.mjs` separates shared design Source authority from exact machine proof.
- Behavioral evidence boundary: `examples/delivery-benchmark/mechanism/**` freezes the old exact-ephemeral default and new assurance-split candidate plus predeclared quality, ownership, verification, honest-gap, false-complete and cost criteria. Static benchmark tests establish experiment integrity only; fresh independent paired runs establish any effectiveness or ROI claim.
- No Long-Task runtime owner changes: `long-task-delivery-v2`, parser/compiler, risk floor, Authority, Evidence Kernel, Progress/repair, Final Gate, External Confirmation and no-drift theorem remain on their existing owners and tests.

## Anti-Degradation Assurance Owners

- Stable purpose, declared-purpose validity floor, capability-claim levels, enabling key logic, two jointly necessary implementation responsibilities, theorem boundary, `F = Implementation Freedom Boundary` and protected design-purpose-change rule: `PROJECT_SPEC.md` and `decision-rationale/long-task-workflow.md`.
- Normative change admission and owner-decision boundary: `contracts/workflow-contract.md`; Context discovery terms: `project_context/context.toml`.
- Current packet-first positive-default, six parent-only reasons and partial-delegation semantics: `PROJECT_SPEC.md`, `contracts/workflow-contract.md` and `decision-rationale/long-task-workflow.md`. The portable operational owner is canonical `long-task-workflow/SKILL.md`; AGENTS/metadata/public docs are short routes and generated/package copies are projections. This is not a second Skill, scheduler, proof carrier or measured host-performance claim.
- Current meaning-capture implementation: Source authoring/classification/continuity modules, Control/relation/applicability and architecture bindings under the Canonical Delivery Contract section below.
- Current interception implementation: Claim/proof-surface policy, target/capability evidence, Counterfactual sensitivity, frozen verifier/authority inputs, revision/freshness and Live Final Gate modules under the Canonical Delivery Contract and Evidence Kernel sections below.
- Executable continuity: `tests/ty-context/long-task-design-context.test.mjs` owns separate causal-chain/current-implementation and `implementation-freedom-boundary` anti-degradation sentinels; `tests/ty-context/long-task-efficiency-design.test.mjs` independently checks the exact positive-default predicates, named solo reasons and no-scheduler/no-proof convergence; semantic assurance and drift suites own independently grounded positive/negative controls for concrete false-completion paths; `tools/test_suite_policy.mjs` owns the one small stable critical mapping, evidence-bounded rationales and derived Trust membership, while `tools/test_suite_lane_policy.mjs` owns the non-duplicating complete-suite Trust/remainder lane projection and fixed per-process file bound; `tools/affected_test_selection.mjs`, package test runners and CI widen changes through affected, Trust and complete coverage. Static tests establish carrier/distribution semantics, not actual host adoption or performance. A real critical incident is frozen before repair, invalid behavior is not a protected baseline, and fresh-Agent benchmark results remain adoption/ROI evidence rather than safety proof.
- Distribution and freshness continuity: `packages/ty-context/source-mappings.yaml`, package `sync-source`/`check-source`, the build fingerprint, Context/harness validation and managed/package parity tests. These are project verification, not Long-Task acceptance authority.

This index describes the actual current repository owners rather than a proposed or “theoretical” implementation. When an owner moves, the same change updates this index and its executable continuity check. No separate mechanism matrix, implementation registry, Gate or runtime state is created.

## Admitted Observer And Comparison Owners

- This subsection records current owner truth and bounded pre-final evidence. The final integration report, not this durable index, owns the exact current Trust/full/package/black-box Final-Gate matrix after the last controlling change; independent-audit and real-workload ROI remain separate promotion evidence.
- Transition evidence: commit `28166f3c` froze the original eight real-lifecycle Observer/TCB counterexamples against inherited `0f35e08a`; commits through `73034d2a` implemented the bounded static/process observer, causal Counterfactual bridge, fixed critical sentinels and fixed-ID black-box delivery proof. Test-only commit `75556705` preserved the later R9 non-carrier evidence input, R10 non-carrier verification input and R11 Contract-consistent but Source-wrong root against `808efa9e`. The current subtraction convergence retains these incidents but separates the R9/R10 must-allow, isolated non-consumption and explicit closure-role-conflict proofs. This is Level 3 known-counterexample protection, not independent audit or complete total-cost ROI; Level 4 remains unavailable.
- Locked threat-model decision and theorem boundary: the Observer/TCB and complete-delivery closure sections in `PROJECT_SPEC.md`, `decision-rationale/long-task-workflow.md` and `contracts/workflow-contract.md`. Ordinary model-authored verifier errors are in scope for material acceptance. Every blocking Claim or Fact × required-method obligation is projected through the existing compiler into one completion route: `package_static_json_exact`, `package_process_json_exact` or exact `external_confirmation`. Observation and reachability projections remain ephemeral and derived; neither is a public Contract Authority, registry, state or second result owner.
- Generic external proof stays on existing owners: `long-task-claims.ts` projects exact `external_confirmations[].impact_claims` into ordinary/global and semantic Claim Coverage and owns the shared target-blocking predicate; `long-task-stage-policy.ts`, `long-task-target-policy.ts`, `long-task-risk.ts` and `long-task-conformance-policy.ts` reuse it; `long-task-semantic-fact-contract-closure.ts` preserves exact per-Fact external bindings; and `long-task-evidence-capability-policy.ts` retains the success-path and causal-capability floors. The complete-delivery amendment adds `long-task-acceptance-reachability.ts` for the derived obligation plan, `long-task-external-confirmation-{types,shape,plan,state}.ts` for strict external Actual/evidence preparation and validation, and `long-task-repair-frontier.ts` for a non-persistent diagnostic projection. `long-task-final-v2.ts` remains the sole current-snapshot status owner while emitting `long-task-final-receipt-v3`; the filename is an internal compatibility locator, not receipt schema authority. No declaration or submitted confirmation independently creates a machine result or terminal.
- Expected authority and method taxonomy remain in canonical Source/design resources, Contract `fact_expectations`, `design-resource-fact-policy.ts` and `design-resource-fact-property-methods.ts`. No observation/compiler/runtime module may own expected values or create a second Fact taxonomy. Compile and runtime must reject an unrecognized/custom Oracle, unsupported proof surface, family/method mismatch, project-only runtime/aggregate record or absent Harness actual channel rather than continuing into the legacy validator.
- Exact comparison/result-identity owner remains `long-task-exact-comparison.ts`. V1 ground, V2 symbolic, non-UI Semantic Fact, static observation and process observation call `evaluateExactDigestComparison`; remaining path-specific entry points are thin wrappers around that owner. Harness-recomputed output is authoritative. Submitted actual/pass/verdict may only be checked for agreement and any disagreement fails closed.
- Material Source conservation is owned by the existing `long-task-source-{item-parser,inventory,markers,continuity}.ts` path plus `long-task-source-fragments.ts` and semantic manifest Census policy. The scanner derives stable paragraph/list/table/Given-When-Then/fenced-code/structured-config fragments and deterministic high-signal anchors. Existing semantic Fact manifest types/shapes/policies own fragment disposition, authority domain, delivery-semantic versus source-integrity classification, lineage and same-domain supersession; no second Source/Fact file or registry exists.
- Proof-adequacy and Expected ownership are enforced by `long-task-proof-adequacy.ts`, existing semantic Fact proof/property policy, `long-task-claims.ts`, assertion shape and observation-authority compilation. Claimless Checks are diagnostic and excluded from semantic completion; claim-bearing binary Expected resolves through explicit upstream authority or a bound Semantic Fact. Evidence capability additions stay in the existing closed enum and codec/schema surfaces.
- `task.target_profile.completion_authority` remains part of Delivery Contract V2 and defaults missing legacy input to `machine_only`; schema/parser/preflight expose the compatibility diagnostic. `long-task-acceptance-reachability.ts` derives exact machine/external/unreachable rows and compatible Session groups before Authority Lock. This output is neither persisted nor a Gate.
- `long-task-external-confirmation-record-v1` persistence stays beneath the existing `.ty-context` runtime boundary through `long-task-external-confirmation-state.ts`, using repository path/no-follow checks, strict canonical JSON/hash validation and atomic publication. CLI routing remains `commands/long-task.ts`; the nested `external prepare|submit|status|revoke` handler does not decide acceptance. Relevant-input identity is derived from existing scoped Bindings/input envelopes and falls back to whole-candidate binding when independence is not sound.
- Final receipt/status/lifecycle ownership remains `long-task-final-v2.ts`, `long-task-state.ts`, `long-task-status-{projection,v2}.ts` and `commands/long-task.ts`. V3 is the only newly emitted receipt, V2 is compatibility-read-only, and only `machine_accepted | delivery_accepted` clears Active Authority. `machine_accepted_external_pending` remains readable historical audit state only. Harness continues to expose `native_goal_effect: none`.
- Stable admitted-observation facade remains `long-task-admitted-observation.ts`; `long-task-admitted-observation-records.ts` remains the record projection; `long-task-json-pointer-observation.ts` remains the strict UTF-8 JSON/RFC 6901/canonical-value and resource-limit owner; `long-task-observation-artifact.ts` remains the containment/no-follow/bounded-acquisition owner. The first slice stays plain, exact JSON with bounded canonical locators and current limits. Browser, Native, device, layout, pixel, accessibility, motion, protected-value, tolerance/mask, custom locator/Oracle and indirect wrapper are not machine adapters and require External Confirmation.
- `prepareExecutionObservationUniverse` prepares and freezes every Raw Execution group's admitted inputs before any group runner starts. Static observation then consumes the candidate-snapshot manifest before it trusts a runner-named path. The carrier must already exist, retain the same regular-file/no-link identity and digest after execution, match a real Binding, be a static implementation/configuration structure, and be disjoint from Source/Context/Contract/expected authority and verifier/evidence/report/status/comparison/Receipt paths. Runner-created, modified, replaced or link-switched carriers fail before their new bytes are used. This owner proves only static structure; it cannot derive runtime, interaction, liveness or render claims.
- Direct-process observation extends the existing runner/execution boundary without a general runtime framework. `long-task-source-target-index.ts` and `long-task-source-target-continuity.ts` own required-target Source projection. `long-task-process-runtime-closure.ts`, `long-task-runner-freeze.ts` and `long-task-observation-authority.ts` compile one declaration-stable internal `CompiledProcessRuntimeClosure`; it binds the Source target, production Binding/owner, exact root, exact Claim/Counterfactual carriers and only argv values admitted by a closed owner-local grammar. `long-task-process-runtime-closure.ts` keeps each child-visible raw token unchanged, examines an independent token or explicit `--name=value`, and derives a separate locator only after rejecting quote-bearing, POSIX/UNC absolute, drive-prefixed absolute/relative, platform-ambiguous slash/backslash, scheme-shaped colon prefixes outside the exact decimal `node:<number>` allowlist and unsupported compound forms. The only other colon scalar is digits-only `<hours>:<minutes>`; arbitrary alphabetic prefixes never become labels. Cwd-aware resolution permits an in-repository parent traversal and rejects a true escape. Exact/pattern production Binding matching supports glob-owned, extensionless and space-containing files; unmatched safe values and those two closed colon scalars are not copied. `long-task-check-runner.ts` remains the exact direct-spawn owner, so classification never becomes a second canonical argv. One role pass checks actual closure members. `input_paths` remains scope/freshness and is neither broadly role-scanned nor copied; no shell/URI parser, extension list, output-flag allowlist, generic dependency parser or manifest/glob sibling expansion exists. Exact planned members may be absent at Preflight/Compile but must exist at Final Gate. `long-task-execution-observation.ts` consumes only that closure; `long-task-check-runner.ts` owns bounded stdout and direct dispatch. `long-task-command-process.ts` routes ordinary direct spawn unchanged, non-Windows contained execution through `long-task-process-{table,tree}.ts`, and Windows contained execution only through the focused `long-task-windows-job-supervisor*.ts` adapter, helper-transport, protocol and result-validation modules. The one adapter locates the package-owned byte mirrors under `assets/runtime/windows-job-supervisor/**`, creates one private request/output root per execution, invokes the canonical PowerShell/C# Job primitive once and validates request, result, stream, timeout/overflow and `ActiveProcesses == 0` closure. This responsibility split satisfies the package modularity boundary without creating another native supervisor or execution adapter. The canonical `tools/formal_process_supervisor_native_{types,run,helpers}.cs` plus `tools/windows_job_process_supervisor.ps1` remain the single native source owner and are byte-equal to package assets; no PID/CIM fallback, wrapper command, helper pool or process registry exists. `long-task-process-observation.ts` owns strict v1 decode. `long-task-check-execution-policy.ts`, Authority hash and Check identity bind closure identity, while `long-task-verifier-v2.ts` enforces current-candidate materialization and `long-task-verifier-identity.ts` includes the four helper assets in the existing bundle. Compatible Cross-Check Facts reuse one Raw Execution/envelope and retain independent exact result identities.
- Global Binding resolution remains distributed through `long-task-activation-validation.ts`, `long-task-observation-authority.ts`, `long-task-process-runtime-closure.ts`, execution/comparison owners and `long-task-counterfactual-claim-policy.ts`, but all consume one internal scoped record `{ outcome_key, local_key, binding_ref, binding }` with `binding_ref = <outcome>.<binding>`. Same local keys across Outcomes remain logically distinct; identical physical carriers may be copied once without collapsing refs; same-Outcome duplicates still fail. Authored Contract Bindings, schema, Authority and state are unchanged.
- Production-carrier and causal sensitivity owners remain Technical Bindings, `long-task-evidence-sensitivity-policy.ts`, `long-task-evidence-v2.ts`, `long-task-execution-observation.ts`, `long-task-verifier-v2.ts` and Final Gate. Static Counterfactual proves only the mutated structure Fact. Runtime reachability additionally requires the same compiled closure identity for baseline and mutation, mutation membership in that closure's production-carrier set, the same Source-backed root and no Authority/verification/evidence member. Expected-affected, preserved and allowed-fan-out sets, equal obligation universe and target liveness remain unchanged.
- Frozen attack/control proof is `long-task-observer-trust-counterexamples.test.mjs` plus `long-task-observer-trust-fixtures.mjs`, supplemented by static/process/execution-observation owner tests. R9/R10 each retain three non-substitutable layers: unused non-closure evidence/verification must-allow, attempted non-closure consumption rejected by isolated execution, and explicit production Binding/argv closure role conflict rejected at Compile. R11 and remaining observer cases continue to reuse `observer-admission-no-bypass`, `host-derived-target-runtime` and `counterfactual-production-observation-impact`; the four Observer/TCB records remain in `CRITICAL_TEST_SENTINELS`. Each attack binds complete committed fixture HEAD/tree/clean identity and its owner diagnostic. Compile-time attacks separately retain Final-Gate legal-neighbor protected-input/freshness proof; `active_task_missing` and a fresh Compile rejection cannot substitute. No second registry or runtime state exists.
- Runtime-capability delivery proof consumes fixed-ID Node machine reports, pass/fail/skip state and the wrong/control candidates' actual workflow statuses. `long-task-real-capability-delivery-machine-report.mjs`, `long-task-real-capability-delivery-verifier.test.mjs` and `tools/verify_long_task_real_capability_delivery.mjs` own that current-invocation pairing. Static token/source probes may prove documentation parity only. Real-process ROI v5 is independently implemented by `examples/delivery-benchmark/real-process-workload/**`, canonical `accounting-policy.json` and `formal-scenario-catalog.json`, the real-process policy/schema/runner/scoring owners, artifact-index/role/packed-package owners, the unique `long_task_package_materialization*.mjs` owner, the JS supervisor protocol plus PowerShell host and three native C# partials, the formal runtime/interaction/collection/execution owners, `long_task_formal_{acquisition_runtime,provider_capture,provider_protocol,provider_worker,state_capture,artifact_budget,collection_readiness}.mjs`, the bounded `long_task_formal_total_cost_*` closure, the sole `verify_long_task_real_process_roi.mjs` formal verifier, and the Level-4 audit/promotion protocol, evidence validation, package comparator and promotion verifier owners. Every one of those source files, `npm_command_spec.mjs` and the owner-local implementation-closure checker is included in `realProcessRoiBenchmarkImplementationPaths`; production collection, Git-object and Promotion identity paths rerun the finite static/literal local dependency closure and bind the actual executing source root. Runtime TCB v2 separately binds the six supervisor host/protocol/native entries plus host PowerShell/.NET/Windows/Node, empty clean Node launch envelope and the fixed Provider parent/worker/protocol adapter identity. `long_task_formal_collection_readiness.mjs` reuses the actual collection validators before A/B/C materialization, derives required price-meter completeness from accounting policy, and reports only formal executability/blockers; the CLI's false pre-evidence ROI Booleans remain top-verifier-owned. The scenario catalog is the sole 11-scenario measurement-source mapping and zero-policy owner; collectors declare capabilities only. A module-private-branded acquisition runtime owns contemporaneous interaction transitions, the Windows Job supervisor, fixed parent Provider bridge, candidate-owned isolated Node `https` worker and parser, bounded deadline/response/diagnostics/abort cleanup, State payload/ledger capture, exact invocation, fresh child-only output roots, candidate before/after observations and raw files. The Windows Job adapter owns create-suspended assignment, inherited process-tree containment, cumulative CPU, timeout/overflow termination, bounded concurrent streams and active-process-zero closure. The top verifier validates the actual run set, creates the immutable artifact index and alone emits formal admission, support, positivity and status; the packet only maps evidence keys to indexed event paths, the evaluator/helpers persist no conclusion and governance audit never becomes a verifier. `invocation_id` is pre-spawn; record SHA and `execution_id` are post-close. V5 derives 86 executions and 586 formal files from the frozen catalog, with 650-file/364.625-MiB formal and 4,379-file/974.3125-MiB complete fuses. The Evidence Candidate owns all code/schema/Context/test/package-version/promotion-protocol bytes. Direct-child promotion can add only the four fixed package-/TCB-external governance records and must preserve exact materialized-package, benchmark and runtime/TCB identities. Provider-event v1-v2, the other listed formal v1 schemas and real-process v1-v4 are legacy/recollection-required. Synthetic incident fixtures test structure and remain `controlled_incident_external_pending`; they cannot prove ROI or promotion. Catalog-driven helpers split the complete 586-artifact fixture across six boundary suites below the touched-source limit, and the prior monolithic-fixture modularity waiver is removed. `long-task-level4-supervisor-controls.mjs` plus its C#/PowerShell probe own dynamic nested-job, breakaway, assignment-limit and helper-crash controls; `long-task-level4-promotion-{controls,fixture}.mjs` owns transient full-authority five-way/unbuilt/mutation controls without persisting a formal-positive artifact. The maximum claim remains Level 3 until authorized real/sanitized-real incident evidence, invocation-bound Provider/price material, State-retention Source, the sole verifier's complete positive report, independent audit and explicit project-owner promotion all close. Package `0.8.15` completes the scoped Level-4 mechanism logic only: capability stays Level 3, `level_4_claimed=false`, and real incident/price/retention/formal-positive evidence, real-evidence audit, owner approval and Promotion remain external pending.
- Provider acquisition is physically split without adding a framework or second owner: `long_task_formal_provider_capture.mjs` owns adapter identity/readiness, `provider_session.mjs` owns the one-shot bridge, `provider_worker_host{,_io}.mjs` owns bounded launch/diagnostics/termination, `provider_capture_io.mjs` owns bounded bridge/artifact I/O, `provider_protocol.mjs` owns the frozen protocol and limits, `provider_worker.mjs` owns `node:https`, and `provider_response.mjs` owns the fixed response parser. `long_task_benchmark_implementation_closure{,_dependencies,_lexer}.mjs` similarly separates identity orchestration, finite dependency interpretation and bounded lexical scanning. All of these exact files are benchmark-identity entries and remain below the touched-source modularity threshold.
- Real-process planning is split at the existing owner boundary: `prepareRealProcessRoiStaticPlan` resolves candidate/tree/clean HEAD, workload/benchmark/accounting identities, precollection Source, platform-independent Provider adapter identity and combined readiness without probing the host; full planning then derives the selected Windows runtime-TCB identity. Dry-run stops at the static result while Source is incomplete and reports a null runtime-TCB digest; a statically complete unsupported host reports `formal_process_supervisor_platform_unsupported`. Collection, runtime revalidation and Promotion still consume only the full plan. `tools/level4_promotion_commit_boundary.mjs` exports the one direct-child/add-only four-record boundary used by the root-locked full verifier and direct temporary-repository tests; it does not read evidence or create another promotion conclusion owner.
- Active package-promotion tests derive package version from `git show <candidate>:packages/ty-context/package.json` and obtain record/tarball/cleanup through one memoized exact-candidate fixture; the critical sentinel performs a second independent materialization. Historical synthetic fixtures retain their explicitly historical versions. Package 0.8.15 is the historical frozen Evidence Candidate identity, package 0.8.18 is the published predecessor, and package 0.9.0 is the current Level-3 package candidate with no inherited formal evidence or Promotion.
- `docs/level4-evidence-candidate-readiness.md` indexes the derived file/byte budget, current structural delta, explicitly non-formal local diagnostics, cost-to-protection mapping, exact external incident/Provider/State inputs and the fifteen-role independent-audit census. It is candidate-owned audit input, never a verifier report, governance record or promotion authority.

- `tools/long_task_formal_total_cost_scenarios.mjs` is part of that bounded helper identity and owns only the fixed scenario-catalog/task/gold/output checks. It is neither a general scenario registry nor a second evidence or conclusion owner.

## DRA Replay, Recovery And Writeback Owners

- Durable authority/replay/recovery/audit boundaries: `contracts/design-resource-authoring.md`, `contracts/temporary-content-governance.md` and the DRA section of `PROJECT_SPEC.md`.
- Managed behavior: `.codex/ty-context-managed/skills/design-resource-authoring/SKILL.md` plus `references/recovery-and-writeback.md`; generated and package copies follow `packages/ty-context/source-mappings.yaml`.
- CLI entry: `commands/design-resource.ts` dispatches the nested `recovery` group without changing bundle/preflight behavior.
- Strict data/semantic owners: `design-resource-recovery-{schema,types,patch-types,codec-primitives,shape,writeback-shape,codec,authority-policy,delta-policy,catalog-shape,catalog,catalog-resources,replay,source-authority,repository-bindings,writeback-policy,text,current}.ts` and `design-resource-reconciliation-{types,codec}.ts` own explicit v4 checkpoint/audit plus exact-patch-v4 types, same-item `ty-dra-authority-v1` scope projection, complete Base/Delta/delegation and active-target/supersession validation, checkpoint-frozen final dispositions, Proposal-only atomic scalar Delta-to-text projection, the package-canonical `ty-dra-proposal-scalar-v1` add/remove carrier, original-preimage UTF-16 code-unit span closure, catalog-exact audit expectations, selected-resource conditions, globally unique per-key decisions and structured selected-Source owners. Replace preserves all non-scalar scaffold; create/update resolves disjoint spans against one preimage and text apply uses descending offsets, while whole-post digest plus calculated output-span readback preserves untouched gaps. Explicit meaning uses exact target equality; delegated non-visual meaning requires one independent fully matching Source item. Reconciliation and helper result semantics remain non-authoritative.
- Filesystem owner: `design-resource-recovery-files.ts` owns raw-byte/encoding/EOL identity, fixed ignored session location, no-follow/collision containment, same-directory exclusive temporary publication, create/update/writeback CAS, readback/idempotence and inventory-first helper-owned/explicit-partial cleanup. `repository-path-safety.ts` is the shared generic path guard; `long-task-protected-files.ts` remains a compatibility re-export and Long-Task authority semantics do not move.
- Orchestration: `design-resource-recovery.ts` creates/updates/inspects/previews/applies/reconciles/removes one explicit session checkpoint. Update uses the caller's expected checkpoint SHA-256 and atomic readback without a registry. Apply exists only for frozen Proposal owners; read-only reconcile revalidates resource-owned-only sessions without a writeback transaction. It does not contact Providers, infer aesthetics, choose resources, validate formal handoff, create acceptance or enter Long-Task.
- Executable continuity: `tests/ty-context/design-resource-recovery.test.mjs`, `design-resource-recovery-safety.test.mjs`, `design-resource-authoring-skill.test.mjs`, temporary-governance/routing tests and affected-test mapping cover deterministic failure paths and distribution parity. `examples/delivery-benchmark/mechanism/admission-set.json`, `admission/**`, the two JSON hidden probes, `runner/admission-{shared,score,score-shared,score-dra,score-build-reuse-buy,aggregate,execute,deterministic,runtime-environment,attestation,evidence,evidence-constants,evidence-records,evidence-validation}.mjs`, `admission_benchmark.mjs` and `.github/workflows/admission-evidence.yml` own separate global-envelope and track-local frozen identities, Fresh-Agent DRA/Build-Reuse-Buy experiments, requested/effective provenance qualification and sanitized exact-tree admission evidence. Deterministic reports distinguish the frozen Fresh-Agent benchmark environment from the observed process platform/architecture/Node/runner, use the package engine as a fail-closed runtime prerequisite and bind both identities to the exact candidate. `fresh-agent-admission-benchmark.test.mjs` proves freeze/scoring/pair-policy/attestation/evidence mechanics, not Agent effect. Raw traces stay ignored; the transport validates complete local records, binds compact pair/aggregate summary projections back to their source-artifact digests, and a retention-bounded CI artifact may persist only those sanitized deterministic/summary/attestation records without creating delivery Authority.

## Symbolic UI Design-Fact Owners

UI symbolic V2 is explicit opt-in; V1 remains the default. `design-resource-v1-capacity.ts` owns stat/bounded-prefix admission before V1 full read or hydration. `symbolic-denotation-{types,engine,dag-builder}.ts` owns the restricted domain-neutral predicate model, manifest-level shared axis partition/predicate and Boolean memo/DAG hash-consing session, evaluation and complexity fuses. `design-resource-symbolic-{compilation,indexes}.ts` seed that one session and build tuple/profile/Rule/Census indexes. `design-resource-symbolic-applicability-{authority,policy,profiles,shape,validation}.ts` owns the versioned package subject-kind/capability algebra, profile projection equality, frozen Inspector custom-property closure, validated-basis instance exceptions and unique logical subject-property disposition without physical N/A rows. `design-resource-symbolic-source-ir-{types,shape,oracle,proof,evaluation,evidence}.ts` owns the canonical non-executable Source representation, strict current-input/scope binding, package-derived Source dependency/predicate/complete-domain semantics and attributable failure evidence; the split keeps parsing/input assembly, proof execution, bounded exhaustive evaluation and result/witness construction independently changeable without creating another Oracle. `design-resource-symbolic-noninterference-{artifact,production-closure,recompute,shape,types,validation}.ts` plus `design-resource-symbolic-static-dependency-validation.ts` require a digest-identified frozen executable Oracle with an exact side/method capability and admit only package-derived dependency-DAG closure, restricted-IR exact equivalence or finite complete-domain exhaustive equivalence. Proof graph/predicate fields are recomputable cache only. The artifact owner fingerprints the actual installed executable module closure and deterministically binds and recomputes Oracle/version/capability, environment, every exact current input, Source-manifest or production target, Rule/certificate scope, omitted axes and method-result identities, verifies immutable artifact bytes and emits attributable failure witnesses; metadata-only or internally self-consistent author proof is insufficient. The production-closure owner admits only its fully decidable non-executable static HTML plus inert JSON input subset and rejects every executable, style, template, event, external-load, interactive, vector or unsupported dependency surface until a complete extractor exists. `design-resource-fact-{enums,types,manifest-shape-evidence,policy}.ts` keeps those proof-Oracle capabilities separate from the Inspector capability catalog while preserving V1 observation-method checks. `design-resource-symbolic-fact-{policy,types,shape,validation}.ts` and `design-resource-symbolic-denotation.ts` own UI Rule/obligation/certificate closure and indexed extensional point meaning. `design-resource-symbolic-{safety,proof-authority,structural-closure}-validation.ts` own closed-only readiness, exact-target per-method region equality, V1 proof-policy reuse and Census/subject/population/resource closure. The existing handoff parser/validation/CLI dispatch by explicit V1/V2 schema without changing V1 semantics or adding readiness state.

Long-Task mixed-target projection stays in `long-task-ui-surface-{types,shape}.ts`, `long-task-ui-design-policy.ts`, `long-task-design-resource-{handoff,method-binding}.ts`, the strict evidence capability codec/policy/runtime and Playwright capability record adapter. The certificate binding/result includes Source and production proof digests only when non-interference proofs exist, preserving older no-omission V2 records while making current production-proof substitution fail in the same Final Gate. The public Contract schema and `public-types.ts`/`index.ts` expose the versioned discriminants and types. Existing Authority, revision, runner, Final Gate and lifecycle owners are unchanged.

Executable continuity is `design-resource-v1-capacity-guard.test.mjs`, `symbolic-denotation-{equivalence,extensional-equivalence,ui-v2,long-task-v2,efficiency-antidegradation,efficiency-guidance,structural-efficiency}.test.mjs`, routed through `affected_test_selection.mjs` and the mixed-representation Trust sentinel. The UI V2 suite sends unresolved/blocker, unproved/dynamic/external/sampled non-interference, stale/missing/current-input-mismatched artifacts, custom-property/derived-profile/exception-basis, proof-policy and exact-region counterexamples through real preflight. It includes source graph, predicate and finite-domain caches that remain internally self-consistent while the current restricted Source IR reads an omitted axis, plus freshness and unsupported-Source cases. The mixed Long-Task suite sends those Source and production current-input counterexamples through Compile and the sole Final Gate source recompile, while retaining package-derived positive Source-closure coverage for all three proof methods. Compact trusted Source proofs can establish that closure, but current UI implementation observation is unsupported and must remain target-blocking External Confirmation rather than reaching `machine_accepted`. Text, interactive-control and asset subjects cannot hide their package-required typography/content, interaction/accessibility or resource/asset property families behind a narrow author-selected profile. The deterministic scale fixture fixes 639 subjects, 217 properties, 53 axes and 5,245 variations, preserves all 138,663 logical subject-property dispositions, forbids 137,385 physical N/A rows and Rule × omitted-axis edges, and budgets actual authoring/preflight wall time, manifest bytes and RSS. Purpose-fulfillment efficiency non-degradation remains package admission evidence rather than terminal delivery law. Non-UI and Provider/P0 admission remain separate scope.

## Sparse Context Workspace / Monorepo Owners

- Durable purpose, read-versus-change distinction, alternatives, compatibility and anti-degradation reasoning: `PROJECT_SPEC.md`, `foundation/context-model.md`, `contracts/workflow-contract.md` and `decision-rationale/minimal-context.md`.
- Current default-start implementation: `packages/ty-context/src/lib/context-default-footprint.ts` computes only the advisory core/default/default-child set and is path-agnostic; `context-manifest-schema.ts` keeps the existing Area/role retrieval schema; `validators.ts` already accepts registered Markdown below any `project_context/**` path. `context-manifest.ts` and managed `context_templates/{context.toml,area.md,architecture.md,verification.md}` teach the optional sparse `project_context/workspaces/<workspace-id>/**` convention with existing `root/context` fields, no empty mirrors and unchanged top-level non-monorepo Areas.
- Portable soft-guidance implementation: `.codex/ty-context-managed/agents/AGENTS_CORE.md` and `skills/context_development_engineer/SKILL.md`; root/generated/package copies follow `packages/ty-context/source-mappings.yaml` and workspace/package sync. README, Chinese README, package README and `docs/examples/minimal-context-sample.md` own human-facing examples.
- Objective extension point: a consumer repository's own changed-path/target-scope checker and verification Context. Tiny Context resolves intended workspace(s) task-locally but adds no generic package-manager/path/import/runtime resolver. Active Long-Task continues to use `long-task-workspace-scope.ts`, declared expected/supporting/forbidden paths and `scope_escape`; the default route creates no duplicate classifier or state.
- Design Authority boundary: `design-md.ts`, `doctor.ts` and authoring Skills continue to treat root `DESIGN.md` as the shared project authority. The sparse Context workspace convention does not implement multiple workspace-local design authorities.
- Executable continuity: `tests/ty-context/workflow-contract-routing.test.mjs` checks guidance/index/parity, unchanged single-Area initialization and a sparse monorepo fixture with a repository-common top-level default, workspace-local Context, top-level shared Context and an unrepresented package-manager workspace. `context-default-footprint.test.mjs` proves the common default does not automatically load workspace-local/non-default Context without treating it as unreadable. Existing Long-Task scope and implementation-freedom sentinels prove that the change did not weaken or duplicate their owners.

## CLI And Profiles

- CLI entry/routing: `packages/ty-context/src/cli.ts`, `packages/ty-context/src/commands/index.ts`.
- Long-Task V2 commands: `packages/ty-context/src/commands/long-task.ts`, `long-task-revision.ts` and `long-task-command-args.ts`; the thin entry router delegates revision compile/diagnosis/approval, projects one hash-bound pending approval summary plus its derived human decision brief, classifies supporting-only Context revisions before derived Progress invalidation, emits `authority_revision_adopted` as a non-completion event that returns to rolling execution, and emits the additive one-time terminal-turn `execution_model_checkpoint` on first Compile.
- Three legacy top-level compatibility command modules remain non-executing tombstones; `commands/index.ts` is their sole active registration owner.
- Profile enable/disable and selection: `commands/enable.ts`, `commands/disable.ts`, `lib/profiles.ts`, `lib/types.ts` and the config parser. `lib/migrations.ts` owns one-time exact-content cleanup of removed compatibility Skills and fails modified collisions to manual review; ordinary sync has no tombstone registry.
- Touched-source modularity: `lib/modularity.ts` owns regression-aware code analysis and excludes only `.work_products/**/delivery-contract.yaml`, because that file is the Long-Task logical Authority rather than implementation code; sibling runners/verifiers under the same workdir remain ordinary auditable source. `check-modularity.test.mjs` proves this exact boundary and `validators.test.mjs` covers the validator carrier.

## Canonical Delivery Contract

- Normative JSON Schema: `packages/ty-context/src/schemas/long-task-delivery-v2/**`; V1 parses only to the retirement error.
- Focused Contract/runtime type surfaces and public barrel: `long-task-contract-types.ts`, `long-task-control-types.ts`, `long-task-semantic-contract-types.ts`, `execution-target-capabilities.ts`, `long-task-runtime-types.ts`, `long-task-authority-types.ts`, `long-task-source-authority-types.ts`, `long-task-evidence-adapter-types.ts`, `long-task-workspace-runtime-types.ts`, `long-task-delivery-types.ts`. Control-field, target/profile/stage/journey/capability types are isolated from the base Contract types so each type owner stays below the modularity export limit; `execution-target-capabilities.ts` is the one enum/type owner shared by handoff and Contract parsing.
- Strict YAML, Compact defaults and shape parsing: `strict-codec.ts`, `long-task-delivery-shape.ts`, `long-task-check-shape.ts`, `long-task-root-shape.ts`, `long-task-outcome-parser.ts`, `long-task-applicability-shape.ts`, `long-task-product-shape.ts`, `long-task-control-fields.ts`, `long-task-delivery-parser.ts`, `long-task-ui-surface-shape.ts`, `long-task-delivery-validation.ts`, `long-task-semantic-assurance-policy.ts`, `long-task-ui-surface-{policy,validation}.ts`, `long-task-ui-design-policy.ts`, `long-task-semantic-drift-migration.ts`. Target profile plus required target refs, runtime-family/root entrypoint/capabilities, ordered stages, exact target/journey/atomic-dimension/Given/ordered-When applicability, complete Control field/relation closure, vertical success/degradation roles, all-of evidence capabilities, Product surface bindings, closed-world Population and typed external-impact declarations live in these canonical V2 surfaces. Old V2 contracts receive actionable manual migration diagnostics rather than inferred proof semantics.
- Non-UI compact persistence: `semantic-fact-compact-{authoring,carrier,parser,support}.ts`, `compact-authoring-support.ts`, `compact-shared-structures.ts`, `compact-shared-structure-{authoring,types,validation}.ts` and `long-task-compact-{authoring,authoring-projections,carrier,parser,primitives,projections}.ts` own shared catalogs/selectors, Fact sets, deterministic parameterized structure families, exact acyclic composite sharing, proof templates, projections, explicit exceptions, bounded materialization and stable-key/current-revision identities. The shared-structure codec emits only byte-beneficial canonical families from already bounded compact rows, recursively templates nested content without template-to-template references, verifies digest/key/boundary/argument closure on read and recomputes the canonical encoding to reject known unpooled equivalents; structure hashes never become Fact or obligation identity. Encoding, authoring application, carrier types and strict materialization validation stay separate owners so the compact mechanism does not concentrate unrelated responsibilities. `semantic-fact-source-parser.ts` and `long-task-delivery-parser.ts` accept exactly one compact or expanded representation and feed the same existing semantic/Contract validators. `tools/migrate_long_task_compact_carrier.mjs` is the package-owned one-way authoring migration and deletes the equivalent expanded serialization in place; it never creates a second Authority backup.
- Structural cost admission: `structural-closure-cost.ts`, `tools/structural_closure_cost_{gate,phase_probe,revision_probe}.mjs`, `tests/ty-context/long-task-structural-closure-cost.test.mjs` and the reviewed fixture baseline own reproducible cost measurement, fixed regression predicates, duplicate-opportunity statistics, one-Fact revision blast-radius and unrelated-axis invariance. Reporting/orchestration, disposable phase timing and revision/axis mutation are separate owners. The tool always writes the passing/failing JSON metrics artifact used by CI; it is package admission evidence and does not create delivery Authority, completion state or a second Final Gate.
- Composition and protected authority: `long-task-boundary-check.ts`, `long-task-authority.ts`, `long-task-authority-revision*.ts`, `long-task-authority-revision-analysis.ts`, `long-task-protected-files.ts`, `long-task-progress.ts` and `long-task-risk-surfaces.ts`; canonical Source/Context/Product/Global revision materials, separated Check/Outcome revision analysis, three-way classification, exact external-confirmation change detection and concise material approval summary live in the authority modules, while `long-task-authority-revision-brief.ts` owns the deterministic non-authoritative human projection.
- Context evolution and Progress retention: `long-task-context-authority-topology.ts` projects selected authority-bearing manifest structure, `context-graph-snapshot.ts` captures selected files, `long-task-context-authority.ts` normalizes legacy snapshots and compares Controlling Context, `long-task-authority-materials.ts`/`long-task-authority-material-diff.ts` distinguish supporting-only revisions, and `long-task-progress.ts` binds Progress to the controlling projection.
- Mandatory Source item/restricted-background/formal-block ownership, marker parsing/inventory/text, exact Risk Fact/Outcome metadata, architecture aspect, Non-completing targets, unified Outcome/Global Acceptance references and canonical-target continuity: `source-line-scanner.ts`, `long-task-source-owned-sections.ts`, `long-task-source-item-parser.ts`, `long-task-source-markers.ts`, `long-task-source-inventory.ts`, `long-task-design-resource-handoff.ts`, `long-task-acceptance-reference.ts`, `long-task-source-target-index.ts`, `long-task-source-target-continuity.ts`, `long-task-source-continuity.ts`, `long-task-source-shape.ts`, `long-task-source-claim-validation.ts`. `source-line-scanner.ts` owns iterative CR/LF/CRLF offset traversal and formal-body slicing without whole-document normalization/splitting. `long-task-source-owned-sections.ts` owns Material Item/restricted-background state and diagnostics. `long-task-source-item-parser.ts` composes those owners with formal-block parsing and returns already-decoded formal values so one adapter/preflight parse decodes each formal YAML/shape once. The separate Long-Task Source-inventory and selected-design validation passes still each parse their own protected snapshot; this optimization does not introduce a cross-owner cache or change Compile authority. `long-task-source-markers.ts` owns marker grammar, risk/architecture metadata and text normalization without creating another inventory.
- Shared read-only Preflight/static Compile safety and repair diagnostics: `long-task-activation-validation.ts`, `long-task-authoring-preflight.ts`, `long-task-authoring-preflight-diagnostics.ts`, `long-task-authoring-preflight-repair-order.ts`, `long-task-delivery-compiler.ts`, `long-task-delivery-preflight.ts` and `long-task-workspace-scope.ts`. The last module owns the pure protected/expected/support/forbidden/unclassified classifier plus the bounded first-enable package-managed protected-input collector across first-lock Preflight/direct Compile and later verification; the former duplicated repository-preflight module is retired.
- Optional Codex host adaptation: `long-task-codex-agent-profile.ts` exclusively owns safe install/update/remove and first-lock exact-file recognition for `.codex/agents/long-task-implementation.toml`. It accepts only the package marker and byte-identical package asset for protected bootstrap, preserves customized collisions, creates no registry/state/runtime and never participates in acceptance. `sync-engine.ts` invokes it from the existing explicit `long-task` profile path.
- Claim/operator/all-of proof, one-Claim Assertion arity, Claim × atomic-applicability × required-surface closure, shared proof-surface parsing, adapter/Raw Execution Observation ownership, stage DAG/gate closure, target/runner/entry/capability compatibility, success/degradation separation, method/cell/fact-identity evidence adequacy, external-impact classification, risk-proportional conformance, Outcome/Global Counterfactual-to-Binding and narrow semantic-mutation/liveness sensitivity policy, authority field policy, planned-path validation, paths/risk, transitive verifier dependency closure and runner freeze: `long-task-claims.ts`, `long-task-claim-proof-policy.ts`, `long-task-required-proof-surfaces.ts`, `long-task-stage-policy.ts`, `long-task-target-policy.ts`, `long-task-semantic-assurance-policy.ts`, `long-task-evidence-capability-policy.ts`, `long-task-evidence-capability-codec.ts`, `long-task-evidence-capability-runtime.ts`, `long-task-conformance-policy.ts`, `long-task-evidence-adapter-policy.ts`, `long-task-observation-ownership.ts`, `long-task-counterfactual-claim-policy.ts`, `long-task-evidence-sensitivity-policy.ts`, `long-task-authority-policy.ts`, `long-task-check-execution-policy.ts`, `long-task-delivery-preflight.ts`, `long-task-verifier-dependency-closure.ts`, `long-task-runner-freeze.ts`, `long-task-risk.ts`, `long-task-paths.ts`. Capability declaration policy, strict payload decoding and current-run record validation remain separate responsibilities.
- Context graph snapshot/path containment/stable JSON utilities are shared package primitives and must not depend on retired runtime.

## Evidence Kernel

- Workspace snapshot and declared command execution: `long-task-workspace.ts` is the compatibility facade; `long-task-git.ts` owns Git process/state primitives and structured failure diagnostics; `long-task-workspace-manifest.ts` owns workspace fingerprint, manifest and exclusion semantics; `long-task-workspace-snapshot.ts` owns fresh-root extraction, EOL preservation, dependency links and lifecycle cleanup; `long-task-check-runner.ts`, `long-task-runner-environment.ts`, `long-task-check-evidence-decoder.ts`, `long-task-evidence-capability-codec.ts`, `long-task-evidence-capability-runtime.ts` and `long-task-verifier-dependency-closure.ts` retain their existing execution/evidence responsibilities. Snapshot extraction materializes skip-worktree entries and permits exactly one retry only after an exit-one `checkout-index` failure with no signal and empty stdout/stderr, successful disposal of the partial root and a mechanically unchanged workspace fingerprint; the retry always uses a distinct fresh root, while diagnosed failure, candidate drift, cleanup failure or a second failure remains fail-closed. Structured evidence decoding carries bounded capability records and scenario execution; the evidence kernel validates record shape, target capability, exact handoff fact-set equality, method/condition/record/primary-observation identity and cross-record invariants before any Assertion can prove its Claims. Runner freeze recursively hashes the supported direct-literal verifier module/config/data graph, canonicalizes literal directory URLs (including repository root) only against explicit candidate/runtime ownership, honors one explicit Playwright `--config` before nearest-config fallback, and rejects unresolved loader or ambiguous-config forms rather than claiming generic reachability.
- Compact semantic result identity remains inside the same Evidence Kernel: `long-task-semantic-fact-{contract-closure,contract-facts,contract-proofs,evidence,shape}.ts`, `long-task-evidence-capability-{types,codec}.ts` and `tools/semantic_fact_delivery_evidence.mjs` require stable Fact/obligation keys plus current revision digests for compact expectations and typed results. The emitter generates one independently located result per obligation, including multiple obligations for one Fact; `tools/verify_symbolic_denotation_efficiency_delivery.mjs` preserves the compact Source digest and revision maps at the real project verifier boundary. Result equality is obligation-key exact before its Fact projection; expanded V1 evidence remains compatibility-readable without acquiring compact completion semantics.
- Declared-only single-AC-per-Test multi-project Playwright decoding, report/instance diagnostics, root-bound design-conformance projection, exact exit-one Counterfactual classification, detailed assertion/applicability evaluation, per-Check review artifacts, passed-Check-only Claim/Population proof and Binding/value-sensitive Counterfactual evaluation with preserved liveness: `long-task-playwright-{case-evidence,evidence}.ts`, `long-task-playwright-counterfactual-policy.ts`, `long-task-check-evidence-decoder.ts`, `long-task-evidence-capability-{types,codec,runtime,policy}.ts`, `long-task-assertions-v2.ts`, `long-task-artifacts.ts`, `long-task-evidence-v2.ts` and the declared-surface-only `long-task-counterfactual-sandbox.ts`.
- Common-dir Active Authority V3 snapshot, old-V2 manual-required boundary, task/revision/identity marker, CAS commit, compiled-cache projection, Authority Revision and audit state: `long-task-state.ts`. Revision diff/detail analysis lives in `long-task-authority-revision*.ts`; `long-task-authority-revision-summary.ts` is the single fail-closed owner for user-decision versus mechanically bounded classification and `long-task-authority-revision-brief.ts` owns the self-contained projection.
- Scoped progress, derived stage/frontier projection, precise target/Source/Claim/applicability/AC findings, status/resume/explain/doctor, targeted verification, stateless scope-only candidate exercise, declared-machine target-qualified terminal scope/no-native-Goal-effect projection and Live Final Gate: `long-task-verifier-v2.ts`, `long-task-verification-preview.ts`, `long-task-authority-revision-diagnosis.ts`, `long-task-progress.ts`, `long-task-status-projection.ts`, `long-task-status-v2.ts`, `long-task-final-v2.ts`, `long-task-final-integrity.ts`, `long-task-freshness.ts`. `long-task-final-v2.ts` remains the sole Gate/receipt owner and delegates its pre/post Check protected-input recompile, raw Contract identity and concurrent workspace/Authority checks to `long-task-final-integrity.ts`; `long-task-verification-preview.ts` owns the zero-execution/zero-write `verify --explain` projection; `long-task-status-v2.ts` owns fact-only stale next-action wording and same-repository `resume` observation ordering.
- Package-owned Hook entry/install/preflight and exact entry-level cleanup: `src/long-task-hook.ts`, `long-task-hook-install.ts`, `long-task-hook-preflight.ts`. The install owner adds one `PreToolUse` matcher for `spawn_agent`/`Agent` plus the exact `SubagentStart` matcher for `long_task_implementation`, safely merges user configuration through no-follow snapshot/compare/atomic publication and emits the host-owned trust-review notice when package definitions in `hooks.json` change. The entry reads only official current-event fields: active-binding PreToolUse allows only exact `tool_input.agent_type` after fresh installed/package profile parity, while exact SubagentStart consumes `agent_type`/`agent_id` and injects a non-authoritative worker boundary. The check is pre-spawn freshness, not atomic identity across the later host load; SessionStart/PostCompact/Stop behavior remains intact.
- No active module may import retired staged-orchestration machinery, Codex/AppServer control, persistent model routing/checkpoint state, process identity/tree, parallel-subagent scheduling or Git worktree orchestration.

## Managed Assets And Skills

- Managed source: `.codex/ty-context-managed/**`.
- Packaged assets: `packages/ty-context/assets/**`.
- Source mappings: `packages/ty-context/source-mappings.yaml`.
- Useful input-refinement semantics live in `long-task-workflow/references/source-authoring.md`; the former standalone authoring pointer is removed through exact-content migration, while pre-existing planning documents remain ordinary Source.
- Historical design Source for the page-level authority upgrade: `docs/page-level-uiux-authority-design-source.md`. It is ordinary navigation/provenance only, not registered Context, current workflow routing or completion authority.
- Design-system/resource authoring: `design-system-authoring/SKILL.md` plus provider/adoption references own explicit Open Design system generation/selection/adoption; `design-resource-authoring/SKILL.md` plus selection/Open Design/downstream references and `references/implementation-feasibility.md` own the conditional style gate, current repository technical-Source read, verified `designSystemId`, live capability matching, actual-render/Design-suitability review, zero-defect direct-selection, material-defect smallest-patch refinement, pre-generation Expected Fact Universe, canonical source/manifest commission, separate feasibility-input commission, complete retrieval/inspection, one-time proposal reconciliation, manifest-backed one-target draft generation and atomic bundle publication. No remembered Provider rank/router is an owner; only an opt-in normalized live trace can prove an execution tier. `design-resource-fact-policy.ts` owns complete-observable-Fact, exact-target pixel, universe equality, frozen Census, atomic-axis/property, no-sampling/truncation, per-Fact expectation/result and no-second-authority invariants.
- Manifest vocabulary and model: `design-resource-fact-enums.ts`, `design-resource-fact-types.ts`, `design-resource-fact-manifest-model.ts` and the compatibility barrel `design-resource-fact-manifest-types.ts` own the 33 condition axes, five variation axes, 16 subject kinds, values, lineage, Inspector/Census and Fact model. `design-resource-fact-manifest-catalog.ts` plus `design-resource-fact-property-methods.ts` own the audited standard atomic-property vocabulary and property-specific proof methods; Inspector-addressable custom properties extend that vocabulary without allowing an unknown observable declaration to disappear.
- Manifest parsing and exact-value access: `design-resource-fact-manifest-shape.ts`, `design-resource-fact-manifest-shape-{inspector,axes,facts,evidence}.ts`, `design-resource-fact-shape-primitives.ts`, `design-resource-fact-value-validation.ts`, `design-resource-fact-locator-validation.ts`, `design-resource-fact-locator-resolver.ts` and `design-resource-fact-locator-extractors.ts` own strict decoding, typed values, local locator resolution and located digests. The canonical resources remain the only exact-value owner.
- Expected Fact Universe closure: `design-resource-fact-manifest-universe.ts` orchestrates `design-resource-fact-universe-{helpers,inspector,subjects,conditions,variations,catalog,facts,proof,assets}.ts`; `design-resource-fact-manifest-validation.ts` parses each manifest once from the verified resource snapshot. Together they prove frozen Inspector identity/input digests, complete Census ownership, explicit dynamic populations and relations, Cartesian Fact Cells, valid exclusions/equivalence, lineage, assets, per-method proof obligations and exact normalized handoff conservation without sampling or truncation. Identity-digest hashing avoids one joined collection string, and expected Fact Cell closure checks generated identities against the actual map without materializing a second full fingerprint array.
- Provider-neutral handoff adapter: `design-resource-handoff-types.ts`, split shape modules, parser/policy and file/dependency modules own the shipped `design-resource-handoff-v1`; the Symbolic V2 peers own the opted-in representation. `design-resource-implementation-feasibility-{types,shape,model,validation}.ts` own one strict shared technical Source model, cycle-safe transitive family closure, V1 explicit-condition and V2 restricted-predicate coverage adapters and unified prose exact-value rejection. The owner-local `design-resource-implementation-feasibility-source-decision{,-projection}.ts` pair owns source-item locators, marked-item acquisition/current text digest, strict `ty-design-feasibility-decision-v1` parsing, V1/V2 applicability digest and exact planned-owner/required-realization/blocker projection matching; it reuses the Source Item parser and creates no generic registry or second authority. `repository-path-safety.ts` owns the shared current contained no-follow directory guard used for observed owner roots. Both handoff shapes expose a compatible optional `technical_feasibility_inputs`; absence normalizes to `[]` with an honest limitation, while new implementation-profile bundle publication requires the target-matching input. `design-resource-handoff-manifest-projection.ts` hydrates the same complete canonical object from one target manifest and preserves the separate feasibility index; older embedded input stays readable. `design-resource-handoff-snapshot.ts` owns protected one-read/hash acquisition reused by canonical and feasibility validation without merging their sets. `design-resource-handoff-bundle.ts` plus `commands/design-resource.ts` validate an explicit canonical manifest set and required feasibility inputs, one compact target draft at a time, publish the complete set to a previously nonexistent directory by same-volume rename and distinguish input-valid/source-published wording from production conformance/readiness. The parser uses an iterative line/offset scanner and reuses the Source parser's already-decoded value. `design-resource-handoff-validation-facts.ts` orchestrates `design-resource-handoff-validation-{fact-cells,fact-records,proofs,resource-closure}.ts`; structure/coverage validators keep `Expected = Canonical = Handoff` unchanged before ready.
- Long-Task projection and current proof: `long-task-design-resource-handoff.ts` consumes normalized handoffs incrementally, composes `design-resource-handoff-set-integrity.ts`, includes declared feasibility files in existing Source/verification-input visibility, preserves prior fail-closed diagnostic precedence and rejects missing/extra/duplicate targets through the existing Contract target set; the compact set owner retains only bounded identities, not full handoffs or a feasibility binding ledger. The owner-local `long-task-design-feasibility-binding.ts`, `long-task-design-feasibility-binding-owners.ts` and `long-task-design-feasibility-source-closure.ts` split derivation orchestration, candidate/root matching and exact Source/blocker closure while jointly deriving cell-to-existing-technical-binding selection, ambiguity/missing/unattributed failures, owner-root containment, exact `path#item-key` Source-Claim continuity and blocker decision/external-confirmation closure from current normalized feasibility, parsed Source Items and existing Contract carriers. Their call-local result distinguishes `feasibility` from `legacy`: modern targets consume only uniquely candidate-matched Binding refs; legacy targets consume none. The surface consumer preserves pure-legacy non-empty-Binding compatibility but applies strict modern-only attribution to every Binding as soon as any modern feasibility target is present. They store nothing and define no Contract type. `long-task-design-resource-method-binding.ts` owns Source-Claim/method/fact-expectation projection and never relaxes general Assertion arity or semantic-Fact single-Claim identity. This remains an adapter resource-lifecycle/modularity optimization, not a Contract, Authority, Outcome, Final Gate, state/recovery or completion change. `long-task-design-target-capabilities.ts` independently derives required execution-target capabilities from methods and all condition axes. `long-task-exact-comparison.ts` owns shared exact comparison/result identity; `long-task-admitted-observation.ts` owns the bounded package-admitted current-artifact extraction boundary; typed evidence modules bind current results and Harness-derived verdict enforcement. Project pass fields are non-authoritative compatibility assertions. `profiles.ts` installs both Skills. There is no provider runtime, copied exact-value authority, universal UI observer, registry, Design Authority or new Gate.
- Production-handoff final closure stays in those existing owners. `design-resource-implementation-feasibility-{types,shape,validation-cells,validation-document,validation-support}.ts` plus `source-decision-projection.ts` own required canonically sorted blocker `substrate_observation_refs`, target-wide unresolved-observation coverage and contextual motion-versus-technical time-value rejection. `long-task-design-feasibility-binding-owners.ts` owns the per-Binding-kind path universe and reuses `long-task-paths.ts::proveRepositoryPatternSubset` to require every actual or planned carrier to be a proven subset of an observed `<root>/**`. `long-task-ui-surface-{validation,policy}.ts`, `long-task-delivery-validation.ts` and `long-task-activation-validation.ts` own the non-persisted complete-call deferral which keeps standalone structure strict and delegates final empty-component closure to `long-task-design-feasibility-binding.ts`. No public Contract shape, Binding kind, Claim kind, Gate, Registry, state or workflow is added.
- Default Workflow and role Skills require manifest routing plus one bounded Context search, risk-proportional material semantic/design understanding when applicable, one externally observable Architecture Deliberation with applicable-quality assessment, then `Context Delta`; Goal-owned implementation uses the shared implementation discipline. Engineering content routing and task-level UI/UX analysis live in their on-demand references and create no new workflow or owner. `context_surface_contract` and `screen-contract.md` remain the durable page-duty/information-action-feedback/topology owners, while `context_uiux_design` remains the durable Design Authority owner. A selected V1 or Symbolic V2 handoff retains exact input preflight; default work opens affected targets/conditions and matching feasibility Source, selects actual owner/strategy in Architecture Deliberation, routes to project checks, repairs failures and reports missing reliable observation as unverified without exact production Fact/Obligation accounting. Active Long-Task alone projects exact sets into existing `fact_expectations`, `fact_results`, Claims/Assertions/bindings and Final Gate. Default Contract Conformance carries the one current-candidate Engineering Quality Conformance including Architecture Conformance.
- Full Long-Task Control/applicability-to-production projection: schema and `long-task-contract-types.ts`, `long-task-control-types.ts`, `long-task-semantic-contract-types.ts`, `execution-target-capabilities.ts`, `long-task-applicability-shape.ts`, `long-task-control-fields.ts`, `long-task-ui-surface-types.ts`, `long-task-product-shape.ts`, `long-task-semantic-assurance-policy.ts`, `long-task-ui-surface-{shape,policy,validation}.ts`, `long-task-ui-design-policy.ts`, `long-task-claim-definitions.ts`, `long-task-claims.ts`, `long-task-source-target-index.ts`, `long-task-authority-materials.ts`, `long-task-authority-material-diff.ts`, `long-task-authority-types.ts`, `long-task-authority-policy.ts`, `long-task-verifier-dependency-closure.ts`, `long-task-runner-freeze.ts` and typed evidence capability modules. Field/relation closure preserves Control meaning; applicability binds each Claim to an atomic target/journey/dimension/condition/action cell; the surface binding groups Controls by owner surface/product target, references Technical route/component carriers and root journey, and binds selected targets plus blocker-required capabilities without a UI lifecycle.
- Design Authority/default-footprint advisory: `design-md.ts`, `context-default-footprint.ts`, `doctor.ts` and their tests. Managed workflow/Skill/template parity and Long-Task UI boundaries are covered by `design-resource-authoring-skill.test.mjs`, `visual-delivery-guidance.test.mjs`, `surface-contract-workflow.test.mjs`, `workflow-contract-routing.test.mjs`, `long-task-schema-parser-parity.test.mjs`, the `target-runtime-non-substitution` and strengthened `selected-design-fact-closure` sentinels, and `tools/verify_design_fact_completeness_delivery.mjs`; `tools/affected_test_selection.mjs` maps every manifest/handoff/Contract/runtime owner back to focused coverage.
- Active Long-Task Skill: `.codex/skills/long-task-workflow/SKILL.md` and managed/package copies. The compact main Skill owns the objective, hard boundaries, activity routing and lifecycle summary, including the unconditional one-time post-Authority-Lock terminal-turn checkpoint; packet-first exact host attempts; packet contents; partial fallback; parent shared-worktree path-envelope review; protected implementation freedom; convergence; and Final-Gate-only completion. Its one-level `source-authoring.md`, `contract-authoring.md`, `evidence-design.md` and `authority-lifecycle.md` references exclusively own atomic input, projection, proof and lifecycle rules; the main Skill does not duplicate those detailed blocks. Managed source, installed copy and package asset are exact generated mirrors.
- Optional Codex implementation worker asset: managed source, package asset and installed `.codex/agents/long-task-implementation.toml` are exact mirrors with a package marker, fixed `long_task_implementation` identity, canonical-TOML-owned model/reasoning values, no `service_tier` override and child-local `[agents].enabled = false`. `long-task-codex-agent-profile.ts` owns pure static TOML schema/policy validation and returns those values without duplicating them, plus exact `.codex`-root gating, non-following path/containment checks, regular-file ownership and atomic install/remove/bootstrap rules. `long-task-hook.ts` reuses that existing read-only bootstrap-path inspection on every active exact spawn; failure denies substitution and returns the packet to the parent. This is configuration for a host-spawned bounded implementation session, not proof that the host selected it or that effective model/reasoning/tier inherited, and not a Harness-created agent, Skill, Worker graph, model route, Authority or proof carrier.
- Source-workspace authoring router: `.codex/skills/authoring/harness_package_design/SKILL.md`; its five on-demand owners are `references/package-surface-and-sync.md`, `default-skill-governance.md`, `long-task-mechanism-admission.md`, `migration-and-release.md` and `test-and-benchmark-governance.md`.
- Default role Skill owners: `.codex/ty-context-managed/skills/context_product_plan/SKILL.md`, `context_surface_contract/SKILL.md`, `context_uiux_design/SKILL.md` plus its non-authoritative `references/task-uiux-analysis.md`, and content-sensitive `context_development_engineer/SKILL.md` plus `references/engineering-design-reasoning.md`. Formal selected Web/App resource authoring is isolated in `design-resource-authoring/references/formal-selected-web-app-handoff.md`, with bounded technical Source/feasibility rules in `references/implementation-feasibility.md`; exploration loads only the lightweight route. Static guidance tests prove routing language, owner boundaries and managed/generated/package parity, not actual host activation, decision quality, cost or ROI.

## Tests

- Delivery Contract Compact/parser/compiler/risk/preflight/Source-target/AC coverage: `tests/ty-context/long-task-delivery-*.test.mjs` plus `long-task-closure-invariants.test.mjs`, `long-task-source-authority-closure.test.mjs`, `long-task-semantic-assurance-closure.test.mjs`, `long-task-schema-parser-parity.test.mjs` and focused authoring/Requirement/Observation tests. These own restricted Source classification, architecture aspect, Control/relation closure, atomic applicability, narrow semantic mutation, method/cell evidence, target capability, full Context, Population-universe and required-surface counterexamples, including the Starward and four previously accepted false-positive models.
- Semantic authority, decision-relevance classification, Rev3-Rev36 replay/guards, revision diagnosis/brief UX, Context evolution, field completeness, conservative pattern containment/overlap and Global Claim coverage: `long-task-context-authority-topology.test.mjs`, `long-task-semantic-authority-revision.test.mjs`, `long-task-authority-revision-classification.test.mjs`, `long-task-authority-revision-replay.test.mjs`, `long-task-authority-revision-diagnosis.test.mjs`, `long-task-context-evolution.test.mjs`, `long-task-authority-field-completeness.test.mjs`, `long-task-pattern-containment.test.mjs`, `long-task-pattern-overlap.test.mjs`, `long-task-global-claim-coverage.test.mjs`. `long-task-paths.ts` owns literal-parenthesis support inside the same safe pattern AST, with no extglob/regex expansion. Authority-revision analysis owns concrete reductions; revision summary/brief owners separately expose verifier-content change, evidence invalidation, changed verifier files and unproven semantic preservation without permitting self-authorization.
- Focused closure: `long-task-evidence-sensitivity-policy.test.mjs`, `long-task-global-evidence-sensitivity.test.mjs`, `long-task-source-risk-binding.test.mjs`, `long-task-non-completing-source.test.mjs`, `long-task-playwright-trust-boundary.test.mjs`, `long-task-planned-counterfactual.test.mjs`, `long-task-final-authority-race.test.mjs`, `long-task-public-contract-example.test.mjs` and `long-task-release-tarball-contract.test.mjs`. The race suite mutates raw Contract, Source, Controlling Context, runner, verification input and workdir fragment after Final Gate starts.
- CLI/Evidence Kernel/Stop real temporary-Git black box: `tests/ty-context/long-task-workflow-*.test.mjs`.
- One-time first-Compile terminal-turn host checkpoint, exact English/Chinese reply guidance, `generic_continue_satisfies: false`, absence of old choice fields/state and non-observability of both the next host message and model change: `tests/ty-context/long-task-model-choice-checkpoint.test.mjs` plus managed-guidance routing assertions.
- Default Context discovery, expandable-read/sparse-workspace target-scope guidance, single-Area compatibility and generated parity: `tests/ty-context/workflow-contract-routing.test.mjs`; advisory startup-set selection remains in `context-default-footprint.test.mjs`.
- Shared engineering-quality occurrence, risk-proportional applicable-quality content, Semantic Fact boundary, no-stage implementation discipline, single carrier/limited Final-Gate claim, Context indexing and managed/package parity: `tests/ty-context/architecture-rationale-guidance.test.mjs`, `workflow-contract-routing.test.mjs`, `orientation-fast-path.test.mjs` and `long-task-design-context.test.mjs`.
- Compact carrier exact closure, expanded/compact equivalence, one Fact with multiple obligations, revision/capacity fail-closed behavior, no-shadow migration and affected/Trust routing: `tests/ty-context/long-task-compact-{authoring,semantic-carrier}.test.mjs`, `long-task-semantic-fact-closure.test.mjs`, `affected-test-selection.test.mjs` and `test-suite-runtime.test.mjs`.
- Capability-aware modularity and compatibility: `source-files.ts` owns included-file and analyzer-capability classification; `modularity.ts` owns JS/TS dispatch, honest unavailable metrics, risk/regression and waiver validation; `modularity-python.ts` owns Python lexical function metrics; `modularity-capability-migration.ts` owns safe upgrade-time removal of legacy heuristic-only waivers; `check-modularity.ts` owns CLI rendering. `check-modularity-capability.test.mjs`, `check-modularity.test.mjs`, `modularity-python.test.mjs`, `modularity-capability-upgrade.test.mjs`, `modularity-guidance.test.mjs`, `upgrade.test.mjs`, `validators.test.mjs` and affected routing cover these boundaries.
- Profile/init/sync/upgrade/package assets: existing focused profile/package/upgrade tests cover `long-task`; the profile/Hook test additionally exercises inactive permissiveness and active missing, invalid, outdated, symlinked and non-regular profile fallback.
- Retired standalone authoring compatibility: `tests/ty-context/retired-authoring-migration.test.mjs`; it rejects a serial authoring phase, proves the former pointer is absent from managed/install/package trees, preserves legacy documents as ordinary Source and distinguishes exact-owned cleanup from modified collision/manual review.
- First-lock/revision workspace classification, read-only execution preview and stale feedback semantics: focused authoring-preflight/compiler/verifier/status CLI suites plus `tests/ty-context/long-task-workspace-scope.test.mjs` and `tests/ty-context/long-task-verification-preview.test.mjs`.
- Design-system/resource authoring, provider binding, source-rich/manifest-backed residual-handoff rules, proposal reconciliation, shared handoff mutations, visual authority and mutually exclusive conformance carriers: `tests/ty-context/design-system-authoring-skill.test.mjs`, `design-resource-authoring-skill.test.mjs`, `design-resource-authoring-provider.test.mjs`, `design-resource-handoff.test.mjs`, `design-resource-handoff-capacity-probe.test.mjs`, `visual-delivery-guidance.test.mjs`, `surface-contract-workflow.test.mjs`, `workflow-contract-routing.test.mjs`, Long-Task design-context/compiler/runtime integration and routing tests. Handoff tests prove same-marker embedded compatibility, manifest-backed hydration equivalence, no repeated Fact arrays, exact manifest target/SHA/generation conservation, byte-ceiling rejection and whole-set atomic publication. `tools/design_resource_handoff_capacity_probe.mjs` takes one protected raw-byte snapshot, scans/parses/preflights that same snapshot, asserts caller-declared normalized counts across every V1 collection plus Source/surface identities, counts each supported formal-block decode once and emits only bounded identity/count/time/CPU/RSS/heap metrics rather than the full handoff result.
- Fresh-agent UI/UX recovery benchmark: `examples/delivery-benchmark/mechanism/{tasks,gold,hidden}/uiux-selected-target-recovery.*` plus its fixture handoff, canonical Screen Contract and selected resources measure controlling-Context recall, canonical Source recall, eight-dimension implementation behavior and observed shared preflight; `tests/ty-context/delivery-mechanism-benchmark.test.mjs` proves the fixture/oracle/routing structure, but only fixed independent paired Agent runs can support an ROI or effectiveness claim.
- Controlling objective, key logic, current implementation roles/theorem boundary, Anti-Degradation Assurance, protected implementation freedom, positive-default multiple-agent convergence, Draft lifecycle, Outcome decomposition, live target-runtime/rolling-feedback guidance and mechanism-admission consistency: `tests/ty-context/long-task-design-context.test.mjs`.
- Efficiency boundaries and affected routing: `tools/test_suite_policy.mjs` owns canonical focused lists, 34 stable critical-semantic records from which 26 unique Trust files derive, reviewed tier/hotspot budgets, explicit 19/58/11 pure/isolated/exclusive file classes and the environment-bound `github-ubuntu-v2` current suite budget profile while retaining `github-ubuntu-v1` as historical calibration; `tools/test_suite_lane_policy.mjs` projects those classes into disjoint Trust/remainder lanes and ordered non-overlapping process batches capped at 16 files. The formal External Confirmation sentinel owns exact fresh per-obligation fulfillment, bounded invalidation and runner-time record immutability in its isolated temporary repository; the four Observer/TCB sentinels reuse the existing registry and one black-box file with distinct canonical tags; `windows-job-pre-resume-containment` independently binds the suspended-create/pre-resume Job boundary; six independent Level-4 sentinels route formal accounting, measurement integrity, authoritative process acquisition, source/State/Provider readiness, package/promotion identity and governance/audit boundaries to their split owner tests. `tools/affected_change_discovery.mjs` owns explicit/local/CI change sources plus the reported local-untracked `.work_products/**` exemption; `tools/affected_test_selection.mjs` owns fail-safe tier selection and aggregate dominance, including the pure Rev3–Rev36 replay for revision-classifier hotspots; `tools/run_affected_tests.mjs` verifies current build identity for `--no-build` and executes one highest plan. `long-task-authority-revision-fixture.mjs` owns the test-only canonical-compiler diagnose projection for bulk revision permutations; `long-task-authority-progress-retry.test.mjs` and `long-task-semantic-authority-revision.test.mjs` retain every scenario/address while existing classification/diagnosis/semantic cases retain real CLI and pending proof. `long-task-verifier-migration.test.mjs` owns one-copy locator rename/exact-byte restoration plus bundle/schema/Hook identity and representative content Verify, while `long-task-verifier-identity.test.mjs` independently binds the byte-current package helper identity. `tests/ty-context/affected-change-discovery.test.mjs`, `affected-test-selection.test.mjs`, `test-suite-runtime.test.mjs`, `long-task-external-confirmation.test.mjs`, `long-task-windows-job-supervisor.test.mjs`, the six `long-task-level4-*.test.mjs` suites, `long-task-qualified-completion.test.mjs`, `long-task-real-capability-delivery-verifier.test.mjs` and `long-task-efficiency-design.test.mjs` prove the policy, owner links, fixed-ID black-box machine report, bounded package-copy/real-boundary shape, disposable fixture Git settings, Git-ordering and routing boundaries.
- Adversarial suites: `long-task-active-authority-continuity.test.mjs`, `long-task-authority-adversarial.test.mjs`, `long-task-assertion-safety.test.mjs`, `long-task-schema-parser-parity.test.mjs`, `long-task-counterfactual-integrity.test.mjs`, `long-task-playwright-ac-evidence.test.mjs`, `long-task-runner-freeze-v2.test.mjs`, `long-task-final-closure-mutation-smoke.test.mjs`, population/environment and Hook relocation tests.
- Semantic-drift regressions: `long-task-semantic-drift-closure.test.mjs` covers target substitution, text-only behavior claims, journey separation, Stage Gate entry/consistency, input propagation and observed external effects; `long-task-semantic-drift-lifecycle.test.mjs` covers bounded conformance, capability revision protection, manual old-V2 migration, derived Stage frontier and terminal target qualification. Both are routed through Delivery-focused, Long-Task-focused, Trust and full suites; revision-to-rolling behavior remains covered by the owning authority revision suites.
- Suite build/runtime infrastructure: `tools/package_build_fingerprint.mjs` owns build-and-stamp plus `--no-build` freshness verification; `tools/test_suite_lane_policy.mjs` owns non-duplicating Trust/remainder projection and the 16-file process-reset bound; `tests/ty-context/run-package-suite.mjs` keeps complete discovery exhaustive and passes suite-required sentinel records into `test-suite-file-reporter.mjs`, which folds exact terminal sentinel coverage plus deterministic top-10 slow-file attribution into ephemeral `test-suite-timing-v2`; only the explicit safe lane may run concurrently while exclusive/unknown stays serial. `long-task-delivery-fixtures.mjs` amortizes immutable initialized seeds without sharing mutable repositories and owns the disposable-only `core.fsync`/maintenance/GC config. `long-task-global-evidence-sensitivity-fixture.mjs` is the single schema-valid Global Claim fixture owner. `long-task-performance.mjs` owns semantic-fixture-first ordering, `default-v1`/`windows-v1` phase/seed/total budgets and unique-execution runner accounting; `workflow-test-entrypoints.test.mjs` and `test-suite-runtime.test.mjs` prevent those owner links, fixture boundaries, canonical revision-projection path and verifier-migration copy/command boundary from drifting. Root scripts expose `test:affected`, `test:long-task:focused`, `test:long-task:trust` and `test:delivery-contract:focused`; package `test:trust:built` runs complete default plus derived Long-Task Trust coverage for PRs, while `test:built`/`npm test` retains complete release coverage for `main` and publish. `.github/workflows/package.yml` and `npm-publish.yml` select the named profile and upload the same-run timing files.
- Consumer/package smoke: `tools/quickstart_smoke.mjs`, `tools/release_tarball_smoke.mjs`, reusable `tools/release_tarball_smoke_fixture.mjs`, `long-task-release-tarball-contract.test.mjs`, `npm run preview:pack`.

## Release And Documentation

- Version sync: `tools/sync_release_version.mjs`.
- Release prepare/publish: `tools/release_prepare.mjs`, `tools/release_publish.mjs`, compatibility wrapper `tools/release_npm.mjs`.
- Trusted artifact handoff: `.github/workflows/npm-publish.yml` owns the one-prepare/one-publish job graph; `tools/workflow_release_artifact.mjs` records the runtime artifact and source commit; `tools/verify_workflow_release_artifact.mjs` verifies the downloaded tarball before publication; `tools/publish_prepared_artifact.mjs` makes same-integrity retries safe; `tools/release_artifact_identity.mjs` owns CRLF/LF-stable lockfile identity and separates source identity from build-environment provenance.
- Release behavior tests: `tests/ty-context/workflow-release-artifact.test.mjs`, `release-flow-scripts.test.mjs`, `launch-readiness-script.test.mjs`, `launch-unblock-script.test.mjs` and `launch-next-steps-script.test.mjs`.
- Public docs: `README.md`, `README.zh-CN.md`, `packages/ty-context/README.md`.
- Stable design: `PROJECT_SPEC.md`; durable source-workspace facts: `project_context/**`.
