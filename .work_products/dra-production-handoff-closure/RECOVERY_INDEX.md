# DRA Production-Handoff Closure — Recovery Index

This is an ordinary repository recovery locator for the current native Goal. It is not Context, Design/Technical Authority, a Delivery Contract, a Gate, workflow state, readiness state or acceptance evidence. It does not activate `long-task-workflow`.

## Authority order

1. current user instruction;
2. S7 CI/evidence-closure amendment between `S7-CI-EVIDENCE-CLOSURE:BEGIN` and `S7-CI-EVIDENCE-CLOSURE:END`, only for platform-applicable critical sentinels, direct Windows CI execution, exact Windows process-identity evidence and Trust budget profile v3;
3. S6 controlling amendment between `S6-CONTROLLING-AMENDMENT:BEGIN` and `S6-CONTROLLING-AMENDMENT:END` where S7 does not supersede it;
4. S5 controlling amendment between `S5-CONTROLLING-AMENDMENT:BEGIN` and `S5-CONTROLLING-AMENDMENT:END` where S6/S7 do not supersede it;
5. S4 controlling amendment in S3 Sections 17.1–17.17 where S5/S6/S7 do not supersede it;
6. earlier S3 execution source and S2 corrections/accepted audit findings;
7. S1 details not superseded by S2/S4/S5/S6/S7;
8. current durable repository Context and Design Authority;
9. code as implementation fact only.

## Immutable input identities

- S1: `attachment-provenance://a1cf06fd-5881-411a-bcca-7067c34efe77/pasted-text.txt`; 1677 lines at Goal creation; SHA-256 `849F33E9D0EC60C28A531F9A683482E4DEB5500687CA9E2FBD92CE03AF8030F9`.
- S2: `attachment-provenance://304e7440-f6fa-416b-933b-a3582acec71b/pasted-text.txt`; 1277 lines at Goal creation; SHA-256 `CA4137FE4B3C0780C17B58826249F1B84E4EDFB936948CE12CDABE3D843A03C3`.
- S3/S5 repository source before S6: `.work_products/dra-production-handoff-closure/EXECUTION_SOURCE.md`; 2360 lines; SHA-256 `6F8D27533FE86CF7503CC81EAFF7DD7F57A1396E3C1F2FE06ADC364527E734B1`.
- S4: S3 Sections 17.1–17.17, the durable execution projection of the current user message titled `DRA Production-Handoff Closure 补开发方案`; fixed implementation baseline `c46e1f59961e8735b2ac76d0534a1d0995f05323`; S4 overrides earlier S3/S2/S1 wherever stricter.
- S5: exact 1320-line source block in S3 beginning at line 1039 and ending at line 2360; original provenance `attachment-provenance://452528f5-e136-4b6a-a205-88532d3b9480/pasted-text.txt`; SHA-256 `1BDAE56602A375EDBCD9845368DD2834B344786AC5F83ACEAE115E623EB64713`; fixed implementation baseline commit `749aef74525368f63f45dbb24890b2547131c0b5`, tree `bcf3b2cbc878b980177a57ed5879d20b83fb453a`, package `project-tiny-context-harness@0.8.17`. S5 is final and overrides S4/S3/S2/S1 wherever stricter.
- S6: exact 1049-line source block at lines 2363–3411, bounded by marker lines 2362/3412; original provenance `attachment-provenance://07dd96da-4df2-41a9-a3cf-83df7725527c/pasted-text.txt`; original CRLF bytes 26585; original SHA-256 `CA3E5E9101ADD483C425F755D3EA405ACBC54A3455358C508A269C080A1A43DD`; repository-normalized LF SHA-256 `75EBD573DF73AC3C067189F5D4BFC094927B5AEF801B8CB7716934C78F5BB99B`. The normalized block is exactly equal to the original after CRLF→LF normalization. The complete Execution Source is now 3412 lines with SHA-256 `A41493F469EF284E223A57C189494463925BF8CE227A63AB8F54388D42F66C34`. S6 overrides S5 only for mixed modern/legacy attribution, Windows pre-resume Job Object containment and complete motion-duration token recognition.
- S7: exact 773-line source block at lines 3415–4187, bounded by marker lines 3414/4188; original provenance `attachment-provenance://793b266c-f7a0-4777-bda5-715083f3b986/pasted-text.txt`; original CRLF bytes 15855; original SHA-256 `EB348358BE874534DDBDDEF12D2F6AD2DA9B18F0C346BE115AEC793DCE88276D`; repository-normalized LF bytes 15082 and SHA-256 `5D2B421BDD552700549206C5C049D3EA132280DB67DE402C9AEDD4F8373E8F20`. The normalized block is exactly equal to the original after CRLF→LF normalization. The complete Execution Source is now 4188 lines, 125514 bytes and SHA-256 `07B36866936A816581C580BF6F53AFC98FDD2C92B94F2BDCB82C62D43B90E03D`. S7 overrides S6 only for the four CI/verification-evidence gaps it names; all S6 product mechanisms and boundaries remain frozen.

The S3 digest recorded in the native Goal objective (`2D486A8E3F9602CB0574A866CA701FD6C0D8F34A2F76CBEC87DC635EABD04F4B`) identified the pre-portability revision and is superseded for repository recovery by the S3 identity above. The Goal objective remains the current-session machine-local lookup owner for the original attachments; repository files deliberately retain only non-resolvable provenance locators.

## S5 hard-precondition snapshot

After `git fetch --prune origin` on 2026-08-24 Asia/Shanghai, the task worktree was clean and `HEAD`, local `main`, `origin/main` and their merge base were all `749aef74525368f63f45dbb24890b2547131c0b5`, with ahead/behind `0/0`; the fixed tree was `bcf3b2cbc878b980177a57ed5879d20b83fb453a`. npm returned `E404` for `project-tiny-context-harness@0.8.17`, `git ls-remote` found no `v0.8.17` tag and GitHub reported no `v0.8.17` release. These facts authorized the unpublished V1 in-place semantic repair; they are not final-candidate evidence and must be refreshed where S5 requires current verification.

The pre-delivery refresh on 2026-08-24 repeated `git fetch --prune origin` from the isolated worktree. Before the delivery commit, `HEAD`, local `main`, `origin/main` and both merge bases were still `749aef74525368f63f45dbb24890b2547131c0b5`, with `0/0` ahead/behind and the baseline present in both main refs. npm still returned `E404` for exactly `project-tiny-context-harness@0.8.17`; `git ls-remote --tags` still returned no `v0.8.17`, and `gh release view` returned `release not found`. The original worktree remained on `main` at `749aef` and had 165 unrelated working-tree entries from its separate task. No original-worktree file, index or local `main` ref was changed by this Goal; only shared `origin/*` remote refs were refreshed.

## Recovery route

1. Read this index and verify the complete Execution Source line count/digest plus the S7 marker positions and normalized source identity.
2. Read the exact S7 block first. It contains the four active CI/evidence gaps, positive/negative matrices, allowed file range, implementation order, package-version rule, final same-candidate CI theorem and audit-report format. Then read S6 for every frozen product boundary S7 does not replace.
3. Reopen `project_context/global.md`, `project_context/architecture.md`, `project_context/context.toml`, `project_context/areas/harness-package.md`, `project_context/areas/harness-package/verification.md`, `project_context/areas/harness-package/implementation-index.md`, `project_context/areas/harness-package/contracts/workflow-contract.md`, `project_context/areas/harness-package/decision-rationale/long-task-workflow.md`, `project_context/areas/harness-package/decision-rationale/architecture-quality.md`, `.codex/skills/authoring/harness_package_design/SKILL.md` with the routed governance references, and `.codex/skills/context_development_engineer/SKILL.md` plus `references/engineering-design-reasoning.md` before further implementation changes.
4. Use S6/S5/S4/S3/S2/S1 only where S7 does not supersede them. Never infer completion from historical Receipts, old generated evidence, focused-only runs or conversation memory.
5. Resume only in `E:/Dev/worktrees/project-tiny-context-harness/dra-ci-evidence-closure` on branch `codex/dra-ci-evidence-closure`, based on `origin/main@9bd0a779db244e33e3cbb5cca4557992923fd9f2`. Do not switch, stage, commit, reset or write the original `E:/dev/project-tiny-context-harness` local-`main` worktree or the prior `scratch-20260824` worktree.

## S7 start snapshot and compatibility decision

- Goal start: a new isolated worktree `E:/Dev/worktrees/project-tiny-context-harness/dra-ci-evidence-closure`, branch `codex/dra-ci-evidence-closure`, clean HEAD `9bd0a779db244e33e3cbb5cca4557992923fd9f2`. `HEAD`, `origin/main` and their merge base were identical, with ahead/behind `0/0`. That commit is the merge commit of PR #44 and is the fixed S7 implementation baseline.
- The original worktree remains `E:/dev/project-tiny-context-harness` on local `main` at `9bd0a779db244e33e3cbb5cca4557992923fd9f2`. The prior isolated worktree remains on `codex/scratch-20260824`; neither is an S7 edit target. Normal remote-tracking refreshes are shared Git metadata, not writes to either checked-out worktree.
- The S7 precondition refresh found npm `E404` for exactly `project-tiny-context-harness@0.8.18`, no remote tag `v0.8.18` and no GitHub Release `v0.8.18`. S7 is therefore an unpublished-package CI/test/Context closure at version `0.8.18`; package source/assets are not selected for change. No npm publish, tag or Release is authorized.
- This is a native Goal under the default Workflow Contract. It does not activate, resume or bootstrap `long-task-workflow`.

## Active S7 work index

1. Platform applicability: extend the existing critical-sentinel record with a canonical optional `required_platforms` field; absent means `linux`, `darwin` and `win32`. Resolve registered/applicable/non-applicable sentinels from the one existing registry. On Ubuntu, the Windows sentinel stays registered and discoverable but a skipped non-applicable result is excluded from required/observed/unexpected/non-passing coverage. On Windows it remains required and fail-closed. Unknown platform values, empty platform lists, duplicate values, non-canonical ordering and malformed records must fail closed.
2. Windows CI: the existing `windows-level4-runtime` job in `.github/workflows/package.yml` must directly execute `tests/ty-context/long-task-windows-job-supervisor.test.mjs` in addition to acquisition and promotion. Workflow structure tests must prove both the positive command list and a negative mutation/removal failure; no source scanning or proxy-only proof counts as execution evidence.
3. Exact process identity: the package-owned Windows runtime fixture must prove distinct valid `root_pid`, `child_pid` and `grandchild_pid` rows for the short-grandchild case, include each identity in post-Job disappearance checks and fail if any row is missing, malformed or duplicated. The helper/parser remains test-owned and is exercised by negative unit fixtures. Production Job Object code is frozen.
4. Trust budget: preserve profiles v1/v2 and add `github-ubuntu-v3` with default 180 seconds, Trust 630 seconds and complete 1200 seconds. Package PR/main CI select v3; invalid/zero/negative/missing suite budgets and unknown profiles remain fail-closed. Exactly 630000 ms passes and 630001 ms fails. Do not change test population, concurrency, retries, caches or sentinel assertions; if the final candidate exceeds 630 seconds, diagnose/optimize slow owners instead of raising the budget again.
5. Preserve every S6 product mechanism and all DRA/Open-Design/feasibility/binding/contract/claim/authority/final-gate semantics. Do not add a second registry, proof system, persistent state, workflow, Authority, Gate or dependency.
6. Final acceptance is same-candidate evidence: focused regressions, package/Context parity, unchanged package bytes, then push a new PR and wait for every required GitHub check. Merge is authorized only after all required checks are green. Do not merge on local-only evidence or a partial matrix.

## S7 architecture, selected Context and incident index

- Architecture Deliberation selected the current suite-policy registry (`tools/test_suite_policy.mjs`), runner/reporter boundary (`tests/ty-context/run-package-suite.mjs` and `test-suite-file-reporter.mjs`), current package workflow job, and test-only Windows runtime fixture/support as the exact extension points. No product runtime owner changes. Context Delta is `required`; durable current-fact owners are `project_context/areas/harness-package/verification.md`, `implementation-index.md` and `decision-rationale/long-task-workflow.md`, with the retrieval design index `docs/test-suite-roi-redesign.md` synchronized. No new Context node, Authority or implementation owner was created.
- Build/Reuse/Buy decision: reuse the single sentinel registry, current reporter schema boundary, Node standard library, existing Windows CI job and package-owned runtime fixture. Do not build a parallel registry/proof service, buy/add a dependency, or modify the canonical/package Job supervisor.
- Alternatives rejected: deleting the Windows Trust sentinel; converting skip into pass; executing a mocked Windows path on Ubuntu; accepting source-text scanning as runtime proof; retaining the current two-PID minimum; widening v2 in place; changing test population/concurrency/retry/cache policy; or changing product containment code.
- Future-change challenge: another platform-specific sentinel must require only `required_platforms` metadata plus the same canonical resolver, not a new platform registry. Compatibility requires absent metadata to retain all-platform behavior and v2 to remain selectable with its historical numeric values.
- Debt addressed at the existing owner boundary: coverage currently conflates registered, applicable and observed sentinel IDs, while the Windows fixture accepts any two logged PIDs. S7 separates those meanings without creating durable state or weakening unexpected/duplicate/misplaced/non-passing checks for applicable sentinels.
- PR #44 final external evidence: the Harness and Windows affected/Level-4 checks were green, while Ubuntu `pull-request` failed. Run `32762415909`, job `97544083128`, produced `long-task-trust.json` with 25 files and 183 tests: 172 passed, zero failed, 11 skipped, wall time 566039 ms against the v2 540000-ms Trust budget. Critical coverage alone failed because the sole non-passing ID was `windows-job-pre-resume-containment`, whose Windows-only test was skipped on Linux. The slowest recorded owners were observer Trust 185830 ms, qualified completion 128348 ms, authority adversarial 72701 ms, profile hook 63268 ms, workflow black-box 61569 ms and symbolic v2 59661 ms. Raw downloaded artifacts remain ignored under `tmp/ty-context/s7-ci-evidence/` and are not repository acceptance artifacts.
- Frozen baseline probe against `9bd0a779`: the current reporter required the skipped Windows tag and returned coverage/test status `failed` with sole `non_passing_id=windows-job-pre-resume-containment`; the Trust registry had 30 required entries and the Windows record had no `required_platforms`. The current runtime fixture source contained both `waitForLogRows(log, 2)` and `pids.length >= 2`, and distinct synthetic `root_pid`/`child_pid` rows satisfied that minimum without any `grandchild_pid`. The package Windows job did not contain `long-task-windows-job-supervisor.test.mjs`. Profile v2 resolved Trust to 540000 ms, classified the observed 566039 ms as `exceeded`, and v3 was absent.
- Four final-form tests were then added before implementation and each failed on the built baseline for the intended reason: platform policy saw `required_platforms` as `undefined` (2.0285 ms); budget policy could not find `github-ubuntu-v3` (1.7354 ms); workflow policy showed the exact missing package Job test in the Windows command (6.6579 ms); and the test-only exact identity helper was `undefined` (1.8469 ms). Dependencies and baseline `dist` were prepared before these evidentiary reruns; earlier module-not-found setup attempts are excluded from product counterexample evidence.

## S7 implementation and pre-freeze verification index

- `tools/test_suite_policy.mjs` remains the sole sentinel registry. Only `windows-job-pre-resume-containment` declares canonical `required_platforms: ["win32"]`; absence remains all-platform compatible. `criticalSentinelsForSuite(suite, platform)` validates the supported `linux`/`darwin`/`win32` enum and filters after suite registration. Its existing export budget remains 24, so the first modularity failure caused by four new diagnostic exports was repaired by reusing existing APIs rather than adding a waiver or splitting authority.
- The runner derives registered and current-host-applicable views from that same registry and passes both to the existing reporter. `test-suite-timing-v2` now reports registered, required, non-applicable, non-applicable-observed and applicable observed IDs. Non-applicable tags are excluded from required/observed/unexpected/non-passing without becoming passed; applicable missing/duplicate/misplaced/unexpected/skipped/failed behavior remains fail-closed. Linux/Darwin/Windows positive and negative policy controls plus empty/duplicate/unknown/non-canonical `required_platforms` mutations are frozen in `test-suite-runtime.test.mjs`.
- `.github/workflows/package.yml` directly runs `long-task-windows-job-supervisor.test.mjs` first in the existing non-advisory Windows runtime command, followed by Level-4 acquisition and package promotion. Its workflow test checks the exact command and proves that deleting the package Job filename fails. Trusted Publishing remains unchanged and still owns its pre-existing two-file Windows boundary.
- The test-only supervisor support owns exact identity parsing and disappearance checks. The runtime matrix declares scenario-specific sets: root/child for short-child, overflow and parallel-overflow; root/child/grandchild for short-grandchild and timeout-tree. Missing keys, repeated keys, shared PIDs and invalid non-positive/non-safe PIDs fail with stable diagnostics before best-effort `finally` cleanup. Every accepted PID is then checked absent. No package source, runtime asset or Job Object implementation changed.
- Budget profiles v1/v2 are byte-semantically unchanged. New `github-ubuntu-v3` is GitHub/Linux-bound and sets default 180000, Trust 630000 and complete Long-Task 1200000 ms; Package PR/main select it, publish retains v2. Missing/zero/negative budgets and unknown profiles fail; 630000 is within and 630001 exceeds. Test population, sentinel count, 19/57/11 classes, concurrency, retry and cache policy are unchanged.
- Focused current-candidate results after convergence: full `test-suite-runtime.test.mjs` passed 14/14 in 26442 ms; full `affected-test-selection.test.mjs` passed 35/35; workflow entrypoints passed 4/4; the real Windows supervisor suite passed 8/8 in 19512 ms, including PowerShell 5.1/7, exact root/child/grandchild matrix, timeout, overflow, parallel isolation, Assign failure and packed helper. The four original counterexamples each passed immediately after its owner repair.
- The first `make validate-harness` found only the test-policy export count 28 above the existing 24 limit. Reusing existing resolver arguments removed those four exports; the fresh validation then passed Minimal Context and audited 10 touched source modules with `warning=0`, `waived=0`. No modularity waiver was created.
- Context changes correctly invalidated the compact Source's three Context input identities. The existing `migrate_long_task_compact_carrier.mjs --sync-source-authority --write` owner updated exactly three manifest inputs, zero Source Claims, zero canonical targets and zero acceptance Assertions; it preserved 113 Facts, 113 obligations, 132 inputs, both byte/line capacities and all semantics. Source and Contract now share manifest SHA-256 `31dcd91e9e82bbc20a281e25b3b1440b0802acedc067053462f9e5581330bae1`; the formerly failing compact sentinel then passed in the complete 5/5 file run.
- Package source/assets, package metadata, lockfile and build-fingerprint owner have zero diff from fixed baseline `9bd0a779`; the rebuilt input SHA-256 remains `853d4c3772d2e42f3b6d585b1be468a4b1376bca1f013f565edcda5858105578` and dist SHA-256 remains `f775977f8290293f3fcc69f897a2ad972310dab1e23ff97e7ca04bbb442398c0`. Version therefore remains unpublished `0.8.18` and package/tarball bytes must remain identical in final pack proof.
- Current affected discovery sees the tracked S7 diff and selects `mode=long-task-suite`, `tier=release-regression`, `requires_build=true`, superseding separate focused/Trust aggregates. This is routing evidence only; the final complete matrices remain pending until the tracked candidate is frozen in Git.
- Pre-freeze command checks passed: package formatting, typecheck, release-version synchronization, build/source parity, active-source portability over 100 current files, DRA three-copy/behavior parity 15/15, structural-cost status `passed`, compact exact closure 5/5, Context validation, touched-source modularity and `git diff --check`. These remain pre-freeze evidence; complete Default/Trust/Long-Task, distribution and external CI acceptance must bind the committed candidate.

### Remaining S7 finalization

- Update the selected Context verification owner before implementation code, freeze all four incident/correct controls, then implement in dependency order: policy/resolver, reporter/runner, Windows identity helper/fixture, workflow direct command, v3 profile and workflow selector.
- Run focused policy/reporter/workflow/Windows checks, package build/type/format/source parity, affected routing and complete required local suites without cache/population reduction. Prove the package source/tarball bytes are unchanged from the fixed baseline.
- Commit and push only `codex/dra-ci-evidence-closure`, open a new PR, wait for the exact commit's required GitHub checks, merge only when all required checks are green, then verify the original local `main` ref/worktree was never switched or written and mark the native Goal complete.

## S7 concurrent-main integration index

- The original S7 implementation was frozen on the isolated branch as commit `e5b966d78f9f1c3aa06a6288b2aa86dd5c800fc3` (`Close DRA CI evidence gaps`). Its local no-cache evidence was: Default 66 files/412 tests with 411 pass and one expected opt-in skip; Trust 25 files/184 pass; Long-Task 87 files/595 pass; real Windows supervisor 8/8; pack/Quickstart/offline/tarball/fresh-repository checks passed. The preserved first Long-Task attempt failed only when an unchanged Playwright fixture reached its exact 30027-ms command timeout; the focused owner then passed and a fresh complete run passed. These results bind `e5b966d7` only and are historical after integration.
- Before publication, another task advanced clean `origin/main` to `e0e28e73827251b3562981fcb26f5ecf5e5d0d57` (`feat: close long-task complete delivery authority`), one commit from the common S7 base `9bd0a779db244e33e3cbb5cca4557992923fd9f2`. The S7 branch is integrating that remote commit with an explicit merge; the original local-main worktree remains read-only and is never switched, staged or written.
- Conflict resolution preserves the new main complete-delivery mechanism, `external-fulfillment-current-authority`, 34 total critical records, 26 Trust files and the reviewed 19/58/11 population, while retaining S7's optional canonical `required_platforms`, Linux/Darwin non-applicability semantics, fail-closed Windows applicability, direct three-file Windows CI command, exact root/child/grandchild evidence and immutable `github-ubuntu-v3` 180/630/1200-second profile. No second registry, proof state, Authority, Gate, workflow, dependency or product Job Object change is introduced.
- The two compact carriers took the new main versions as their generated base and were regenerated only through `migrate_long_task_compact_carrier.mjs --sync-source-authority --write` after the combined Context owners converged. The first invocation correctly failed against stale pre-main `dist` on the new `completion_authority` field; after `npm ci` and a 0.9.0 build, the migration updated exactly three manifest inputs, zero Source Claims, zero canonical targets and zero acceptance Assertions, preserving 113 Facts, 113 obligations and 132 inputs with final manifest SHA-256 `d51054a1340181eacde64c04a7e9d5a7e38fc12db8b3f3997eb20bb042e7156c`. The regenerated compact suite passed 7/7. This preserves the complete-delivery Source while refreshing final Context identities; the generated carriers were not hand-merged.
- Package version `0.9.0`, its source/assets, lockfile and release documentation are inherited unchanged from `origin/main@e0e28e7`; S7 neither authors nor publishes that package version. Final review must compare against the integrated `origin/main` and prove that the S7 PR has zero package-byte diff. All earlier `0.8.18` package identities remain accurate only for the original `e5b966d7` S7 candidate.
- The merge commit becomes the sole final S7 candidate. Build, policy/Context/compact checks, complete Default/Trust/Long-Task suites, real Windows protocol, distribution smoke and the PR's exact GitHub checks must be rerun against it before merge; no pre-integration result authorizes acceptance.
- Pre-commit integration checks passed against the combined working tree: policy/reporter/affected/workflow plus real Windows Job 61/61, formatting, TypeScript typecheck, 0.9.0 version synchronization, package source mapping, Minimal Context, active-source portability over 100 files, Harness validation over 133 touched modules with zero warnings/waivers, structural-cost gate, affected release-regression routing, DRA parity 15/15 and `git diff --check`. Package and lockfile diff against `origin/main@e0e28e7` is empty. These are convergence checks only; final acceptance still requires the committed-candidate aggregates and external PR checks above.

## S7 first PR external CI counterexample and repair index

- PR #45 first tested integrated candidate `8a5bcb990a80ff70956f98f3ed5cdd7541be29df`. Harness Gates passed, both Windows affected-launcher jobs passed, and both Ubuntu Package CI `pull-request` jobs passed under `github-ubuntu-v3` in 10m16s and 10m30s. The exact candidate was nevertheless non-mergeable because both independent `windows-level4-runtime` executions failed: pull-request run `32790130821` job `97629875792` after 8m36s and push run `32790100927` job `97629786113` after 9m21s.
- Both Windows runs produced the same counterexample. Level-4 acquisition passed; all package-promotion tests passed; the real `[critical:windows-job-pre-resume-containment]` test passed; Assign-failure and packed-package Job helper tests passed. Four protocol assertions then failed with `process_observer_windows_job_result_invalid:stream_reparse`: the ordinary stream fixture, both PowerShell 5.1/7 compatibility children, and their parent aggregate. The job summary was 18 pass, 4 fail out of 22. No rerun was used to hide the deterministic failure.
- Root cause is test-fixture topology, not the product Job Object boundary. `spawnWindowsJobCommandOnce` already calls `realpath` on each newly created private temporary root before deriving stream paths. The older protocol fixture instead derived stream paths directly from the lexical `os.tmpdir()` result. GitHub Windows exposes its runner temporary path through a reparse/junction, so the fixture accidentally tested its parent runner topology and the existing fail-closed stream check correctly rejected it. A local Node 24.19 replay on an ordinary temporary path passed; a synthetic local temporary-directory junction then proved `path.resolve(created) !== realpath(created)` and reproduced the topology distinction.
- The repair is test-only: `long-task-windows-job-supervisor-protocol-fixture.mjs` now resolves each freshly created fixture root before deriving stream paths, matching the production boundary without changing `readWindowsJobStream`, product temporary-root validation, Job assets, package source, lockfile, platform applicability or sentinel semantics. Under the synthetic junction and Node 24.19, the affected protocol and PowerShell cases passed 4/4. A broader Node 24.19 probe reached 21/22 passes, including protocol, PowerShell, promotion and real containment, before the packed test hit an unrelated local `spawn EINVAL`: the `npx`-delivered Node executable has no sibling `node_modules/npm/bin/npm-cli.js`, so the repository helper correctly falls back to direct `npm.cmd`, which that wrapped Node cannot spawn. GitHub's official Node layout passed the same packed test in both first-PR jobs. This local `npx` layout mismatch is excluded from product/root-cause acceptance; the repository's normal Node runtime remains the local exact-command owner.
- This repair changes the candidate, so every earlier complete local aggregate and the first PR check set remain historical. Acceptance requires a new commit, fresh no-cache Default/Trust/Long-Task evidence, the exact three-file Windows command, package/Context/distribution parity, and a completely green PR #45 check set for the new head before merge.
- The first fixture-repair candidate was commit `56624094ea8e834e92e8cf17f4baad51f75e4e02`. Its same-commit local evidence passed Default 411/412 with the one expected opt-in skip, Trust 186/186, Long-Task 604/604, the exact Windows three-file command 22/22, short gates, distribution smoke and fresh-repository double-sync idempotence. On PR #45, Harness, both Windows affected launchers and both Ubuntu Package CI jobs passed; Ubuntu completed in 10m04s and 11m00s under `github-ubuntu-v3`. One Windows Level-4 job also passed all 22 tests in 11m19s (run `32803884040`, job `97670055167`). The duplicate push-triggered Windows job did not: run `32803886225`, job `97670061119`, reached 21 pass, zero assertion failures and one cancellation after the first package-promotion test hit its exact 300000-ms test timeout at 300003.754 ms. The same test took 148102.238 ms in the successful job; later materialization controls in the slower job still passed at 192436.942 ms and 436571.536 ms. This is preserved as a real concurrent-run resource-contention counterexample, not hidden by rerunning the successful duplicate.
- The follow-up is test-infrastructure-only: the unique two-materialization package-identity test receives a 600000-ms outer test budget, matching the adjacent materialization test. It still performs both independent current-candidate materializations and the same five-way identity assertions; no assertion, Sentinel, product command timeout, package materializer, Windows Job boundary, fail-closed behavior, suite population or CI entrypoint changes. This candidate change again invalidates all earlier same-commit acceptance. A new frozen commit must repeat the affected local verification and obtain a wholly green exact-head PR check set before merge.

## S6 start snapshot and compatibility decision

- Goal start: worktree `E:/dev/worktrees/project-tiny-context-harness/scratch-20260824`, branch `codex/scratch-20260824`, clean HEAD `e09721bebbdfafb6bdc76bb521dba9559610997f`. It contains fixed S6 base `92c49dc9557ba2f2a674e609d5d03fdd292db304`; their merge base is exactly `92c49dc`. The local task branch was two commits ahead of its still-published upstream feature ref and pointed at current `origin/main`.
- The original worktree remains `E:/dev/project-tiny-context-harness` on local `main` at `749aef74525368f63f45dbb24890b2547131c0b5`; this Goal must not switch, stage, commit, reset or write it.
- The S6 precondition refresh found `project-tiny-context-harness@0.8.17` on npm, tag `v0.8.17` at `e09721b`, and published GitHub Release `v0.8.17`. Therefore the S6 compatibility branch is mandatory: all package/version/release surfaces for this repair target `0.8.18`; no released `0.8.17` protocol meaning may be silently rewritten.
- This is a native Goal under the default Workflow Contract. It does not activate, resume or bootstrap `long-task-workflow`.

## Active S6 work index

1. Mixed attribution: legacy targets contribute no feasibility Binding consumption when a surface contains any modern feasibility target; pure-legacy compatibility remains unchanged. Every strict-surface Binding must be consumed by a modern candidate-bearing cell, and the complete Compile matrix in S6 is mandatory.
2. Windows containment: reuse the canonical `tools/formal_process_supervisor_native_*.cs` plus `tools/windows_job_process_supervisor.ps1`, mirror them byte-for-byte into package runtime assets, add one bounded package adapter/protocol, dispatch Windows contained execution through suspended-create/pre-resume Job assignment, freeze assets into existing verifier identity, remove PID/CIM authority claims and prove real Windows root/descendant/timeout/overflow/parallel/tarball behavior.
3. Duration: one validator recognizes compact, spaced and full English millisecond/second units across all four feasibility prose carriers; motion wins over technical context, explicit technical time remains valid and context-free time fails closed.
4. Preserve all twelve S5 completion boundaries and Open Design quality behavior. Do not add any Authority, Contract field family, Binding/Claim type, Gate, Registry, state machine, workflow, Provider ranking/routing, visual benchmark or extra Open Design test.
5. Version, Context, package/source mappings, affected routing, public docs, compact/admission identities and recovery data must converge on the final candidate. Exact selected owners and current-candidate verification evidence are appended here as implementation proceeds.

## S6 architecture and selected Context index

- Architecture Deliberation selected the existing extension points: target-local feasibility validation plus surface aggregation for attribution; the canonical `tools/formal_process_supervisor_native_{types,run,helpers}.cs` and `tools/windows_job_process_supervisor.ps1` for Windows containment; the existing unified feasibility prose validator for duration classification; and `VerifierIdentityV2.bundle_files` for runtime-helper freshness. Context Delta is `required`.
- Build/Reuse/Buy decision: reuse the existing suspended-create Job Object primitive and standard Node/PowerShell capabilities through one bounded package adapter. Do not build a second native supervisor or PID/CIM patch, buy/add a runtime dependency, create a helper pool/registry/scheduler, or wrap the product root through a shell/project command.
- Selected durable owners read and updated before tests/implementation: `project_context/areas/harness-package/contracts/design-resource-handoff.md`, `project_context/areas/harness-package/contracts/workflow-contract.md`, `project_context/areas/harness-package/implementation-index.md` and `project_context/areas/harness-package/verification.md`. Core `project_context/global.md`, `project_context/architecture.md`, `project_context/context.toml`, Harness-package Context and the required package-design/engineering Skill references were read; no DRA Skill change is currently selected.
- Dependency/resource lifecycle: modern/legacy attribution remains one call-local derived result; each Windows contained execution owns one helper plus a fresh private temp/output root through active-process-zero and stream closure; package helper assets are byte mirrors and existing verifier bundle inputs; Contract, raw Contract, host-attestation shape and sole Final Gate remain unchanged.
- Quality checks are fail-closed short-root/descendant containment, timeout/overflow cleanup, request/stream identity, PowerShell 5.1/7 portability, canonical/package/tarball parity, verifier freshness, pure-legacy compatibility, strict mixed attribution and four-carrier duration classification. Forbidden shortcuts are recorded in S6 and the Goal objective.

### Frozen S6 counterexample evidence before product repair

- Full `compileDeliveryContract` mixed fixture passed unexpectedly when modern feasibility consumed only `design-owner`, a legacy target was present and the same surface also declared `legacy-only-owner`; the new `assert.rejects(...feasibility_component_binding_unattributed...)` therefore failed with `Missing expected rejection` (targeted run duration 13.625 s).
- Real Windows direct-process test returned `execution_status=completed`, exit 0, package observations and host attestation for both `short-descendant` and `short-grandchild`, even though each chain left a long-lived descendant. The aggregate negative failed in 12.781 s; cleanup was test-owned and completed. This is the preserved pre-repair proof of the PID/CIM first-snapshot escape.
- The four-carrier duration matrix failed immediately because `Animation lasts 200 ms` raised no `exact_visual_value_forbidden`; targeted run duration 0.427 s. Existing compact forms still passed before the new cases.
- Commands were fresh `node --test --test-name-pattern ...` invocations against the built pre-repair product. The first combined attempt also exposed and then removed two fixture-only defects (duplicate target Assertion and shared fixture mutation); those setup failures are not counted as product counterexamples.

## S6 implementation and pre-freeze verification index

The implementation below is present in the isolated task worktree and remains subject to the final frozen-candidate aggregate. These focused results are owner-level convergence evidence, not a substitute for the complete S6 verification theorem.

### Mixed attribution

- `long-task-design-feasibility-binding.ts` returns a call-local `attribution_mode: feasibility | legacy`. Modern targets consume only refs produced by actual unique candidate matches; legacy targets consume `[]` and gain no feasibility claim.
- `long-task-design-resource-handoff.ts` aggregates that result per existing surface. Pure-legacy surfaces skip the new theorem while preserving historical non-empty-Binding compatibility. Any surface with at least one modern target requires every component Binding ref to be consumed by a modern candidate-bearing cell; shared modern matches remain valid and legacy cannot launder an extra ref.
- The final focused `long-task-delivery-compiler.test.mjs` run passed 25/25 in 258407 ms. Its complete Compile cases cover modern+legacy allow, modern blocker-only+legacy empty/non-empty failure, modern candidate+legacy empty failure, pure legacy non-empty allow, pure legacy empty fail closed, shared modern consumption, unattributed B2 rejection, candidate+blocker `blocked_external`, raw Contract byte preservation, parsed-current raw hash equality and semantic/raw freshness drift.

### Windows pre-resume Job containment

- The single native owner remains `tools/formal_process_supervisor_native_{types,run,helpers}.cs` plus `tools/windows_job_process_supervisor.ps1`; the four package files under `packages/ty-context/assets/runtime/windows-job-supervisor/**` are source-mapped byte mirrors.
- The focused `long-task-windows-job-supervisor*.ts` modules keep one execution adapter rather than adding another supervisor: `long-task-windows-job-supervisor.ts` owns dispatch/result projection, `-helper.ts` owns the single bounded PowerShell transport, `-protocol.ts` owns request and stream protocol, and `-result.ts` owns strict result decoding. Each execution still receives one helper and one fresh private request/output root. Windows contained dispatch has no wrapper/shell/PID fallback. `long-task-process-{table,tree}.ts` now rejects Windows use and retains only the non-Windows process-group/tree path.
- The canonical helper is PowerShell 5.1/7 compatible, rejects duplicate/unknown/missing fields and invalid environment entries, preserves zero/one/many argv values and UTF-8, creates the product suspended, assigns it to a private `KILL_ON_JOB_CLOSE` Job before resume, terminates an unassigned suspended product on Assign failure, and returns only after streams close and Job accounting reports zero active processes.
- Existing `VerifierIdentityV2.bundle_files` includes all four helper assets; a helper byte change reaches the existing `verifier_changed_after_compile` path. No new verifier, Authority, state or dependency was added.
- After the responsibility split, the focused Windows supervisor run passed 7/7 in 21004 ms: strict protocol/streams; real PowerShell 5.1 and 7 UTF-8 requests; immediate root, ordinary child, nonzero root exit, short-root/long-child, short-root/short-child/long-grandchild and full root/child/grandchild cleanup; timeout; over-2-MiB overflow; parallel Job isolation; injected Assign failure with no resumed marker/survivor; and an extracted packed tarball using its own package helper. Verifier identity passed 2/2, direct observation passed 3/3, package source passed 1/1 and test-suite policy passed 13/13 against the same split candidate.
- The serial affected-owner run passed 30/30 in 144056 ms, including direct observer dispatch, the independent formal Level-4 acquisition Job/TCB attacks, state/resume freshness, exact helper bundle drift and package source mapping. Production/current Context contains no PID+CreationDate or CIM containment authority; remaining CIM probes are test-owned external survivor checks only.

### Duration, package and governance convergence

- The one feasibility carrier validator recognizes `milliseconds?|msecs?|ms|seconds?|secs?|sec|s` with optional spacing across observation reasons, realization costs, realization risks and blocker descriptions. Motion context wins; explicit build/compile/bundle/generation/CI/test/startup/initialization/latency/timeout/network/benchmark/runtime/render cost context is allowed; context-free time fails closed. The focused four-carrier matrix passed inside the 30/30 owner run while existing color/size/CSS/custom-property and compact-duration cases remained green.
- Package/version surfaces now target `project-tiny-context-harness@0.8.18`; the published `0.8.17` packet remains historical and unmodified. `docs/launch/github-release-0.8.18.md` describes only the S6 changes and preserves Level-3/Final-Gate/Open-Design boundaries. No npm publish, tag, GitHub Release or local-main merge is authorized.
- Test policy now owns 33 critical sentinels, 25 Trust files and explicit 19-pure/57-isolated/11-exclusive classes. `windows-job-pre-resume-containment` is required by Long-Task and Trust. `test-suite-runtime.test.mjs` passed 13/13 and `affected-test-selection.test.mjs` passed 35/35; the new runtime, protocol, helper, mapping and verifier owners route to their direct/Level-4/identity/package checks.
- Current pre-freeze package build fingerprint is input SHA-256 `853d4c3772d2e42f3b6d585b1be468a4b1376bca1f013f565edcda5858105578` and dist SHA-256 `f775977f8290293f3fcc69f897a2ad972310dab1e23ff97e7ca04bbb442398c0`. Typecheck, build and format passed after the responsibility split. `make validate-harness` audited 29 touched source files with `warning=0`; no modularity waiver was added.
- Compact/admission refresh was evaluated against the converged Context rather than assumed. The repository migration owner updated seven changed Context input identities without changing the 113 Facts, 113 obligations, 132 inputs, source/contract capacity or semantic assertions. Compact Source and Contract now share manifest SHA-256 `74f7a38f74f154764bc9f56727bcc389b876e90bdc948a634b098c34f2694cd2`; the focused compact suite passed 7/7. Admission remains independent of these compact files: freeze-check returned 26 files, zero failures and unchanged track identities `c7e8c6c9f1b691a204f6fd1bf5980e79f1a43ca7eb1b9dc07ec1cc9c21b24be8` (DRA) and `1b1042169380c7209cc999406ab62aa830f59e534266bbd97577441297a5c3c0` (Build/Reuse/Buy).

### Preserved pre-freeze aggregate findings

- The first post-split affected run used the still-unrefreshed compact Source and stopped when `compact-carrier-exact-closure` compared the old committed `delivery-benchmark` Context digest `7fb2e78f...` with the current `0.8.18` Context digest `44319e65...`. The repository migration above repaired that deterministic identity gap; the same critical test then passed standalone and in the complete Long-Task runner.
- The first serial Long-Task run after compact refresh reached `long-task-level4-package-promotion.test.mjs` and correctly rejected the uncommitted candidate because the four changed canonical supervisor entries in the working-tree benchmark/runtime-TCB identity were absent from `HEAD`. The negative case therefore reached `level4_promotion_benchmark_runtime_tcb_identity` before its later package-identity assertion. This is the expected clean-candidate freeze boundary, not authorization to reorder or weaken either check. Complete final suites must run only after the isolated branch commit freezes those bytes.

### Remaining finalization

- Freeze the final tracked candidate only after recovery/docs/hygiene converge, then rerun all S6-mandated complete suites and distribution/smoke/parity checks with no cache or population reduction.
- Preserve any first transient failure and isolate it before a single fresh complete rerun. Do not convert focused evidence above into final acceptance.
- Commit and push only `codex/scratch-20260824`; verify the original local `main` worktree/ref remains untouched, then mark the native Goal complete.

## S5 delivered closure index (subject to S6 overrides)

The final candidate repairs the existing DRA/Long-Task boundary without introducing a second Design/Technical Authority, production Binding type, Contract field family, Claim type, Gate, Registry, state machine or workflow:

- `substrate_observation_refs` is required, unique and canonical on implementation-feasibility blockers. Its projection to Source decision items is exact, and every unresolved observation covers every affected family-by-condition material cell. Stale, missing-family and zero-cell projections fail closed.
- Candidate cells require non-empty observed owner roots. Every actual `file`, `path_glob`, `verified` or `planned` production Binding path/pattern must be a proven subset of an observed owner root; `not_subset` and `unknown` fail closed, and planned carriers cannot be empty.
- Blocker-only targets may traverse the complete in-memory Activation/Compile path with `component_binding_refs=[]`; the standalone structural validator remains strict. Source-aware closure accepts only all-blocker-only deferral, then the existing `decision_required` or target-blocking External Confirmation boundary produces `blocked_external`. Only affected blocked design assertions and matching counterfactuals are projected out of machine execution; the raw Contract and sole Final Gate authority are unchanged.
- Mixed-target attribution is aggregated per target, freshness reparses the current raw Contract, and legacy/route-root/candidate-plus-blocker paths remain fail closed. Permanent visual values, motion duration semantics and explicit technical time costs are separated; unqualified time-like design values fail closed.
- Historical S5 behavior matched Windows processes by PID plus creation identity and closed process trees through polling modules. S6 supersedes that Windows authority with package-owned pre-resume Job Object containment and requires removal/shrinking of the Windows polling path. Explicit `TY_CONTEXT_LONG_TASK_ISOLATED_CONCURRENCY=1` still runs one Node process per selected Long-Task test file; the default suite policy remains unchanged.

### Runtime and validator files

- `packages/ty-context/src/lib/design-resource-implementation-feasibility-shape-sections.ts`
- `packages/ty-context/src/lib/design-resource-implementation-feasibility-source-decision-projection.ts`
- `packages/ty-context/src/lib/design-resource-implementation-feasibility-types.ts`
- `packages/ty-context/src/lib/design-resource-implementation-feasibility-validation-cells.ts`
- `packages/ty-context/src/lib/design-resource-implementation-feasibility-validation-document.ts`
- `packages/ty-context/src/lib/design-resource-implementation-feasibility-validation-support.ts`
- `packages/ty-context/src/lib/long-task-activation-validation.ts`
- `packages/ty-context/src/lib/long-task-delivery-validation.ts`
- `packages/ty-context/src/lib/long-task-design-feasibility-binding-owners.ts`
- `packages/ty-context/src/lib/long-task-design-feasibility-binding.ts`
- `packages/ty-context/src/lib/long-task-design-feasibility-source-closure.ts`
- `packages/ty-context/src/lib/long-task-design-resource-handoff.ts`
- `packages/ty-context/src/lib/long-task-freshness.ts`
- `packages/ty-context/src/lib/long-task-ui-surface-policy.ts`
- `packages/ty-context/src/lib/long-task-ui-surface-validation.ts`
- `packages/ty-context/src/lib/long-task-check-runner.ts`
- `packages/ty-context/src/lib/long-task-command-process.ts` (new)
- `packages/ty-context/src/lib/long-task-process-table.ts` (new)
- `packages/ty-context/src/lib/long-task-process-tree.ts` (new)

### Routing and regression files

- `tools/affected_test_selection.mjs`
- `tools/test_suite_lane_policy.mjs`
- `tests/ty-context/run-package-suite.mjs`
- `tests/ty-context/affected-test-selection.test.mjs`
- `tests/ty-context/design-resource-authoring-skill.test.mjs`
- `tests/ty-context/design-resource-handoff-fixture.mjs`
- `tests/ty-context/design-resource-implementation-feasibility.test.mjs`
- `tests/ty-context/long-task-delivery-compiler.test.mjs`
- `tests/ty-context/long-task-direct-process-observer.test.mjs`
- `tests/ty-context/long-task-pattern-containment.test.mjs`
- `tests/ty-context/test-suite-runtime.test.mjs`

The regression matrix includes blocker-only external/decision/missing-boundary cases; planned-owner full Compile; mixed target attribution; stale blocker and decision blocker cases; legacy and route-root non-applicability; candidate-plus-blocker closure; every-path pattern containment; visual/motion/technical duration classification; freshness against the current raw Contract; affected-routing promotion; and same-PID/different-creation-identity observation.

### Synchronized public, package and compact-authority surfaces

- DRA guidance is byte-identical at `.codex/skills/design-resource-authoring`, `.codex/ty-context-managed/skills/design-resource-authoring` and `packages/ty-context/assets/skills/design-resource-authoring` for `SKILL.md` and the three changed references. The SHA-256 values are respectively `DB126D1C4A5597AF2DA7C0B2F00C86263BC548BD46205002CA25C092959B2C66`, `29E0D274124C1EDD8E2908438A5D369284606716A516CDEB00DA4583A75E488E`, `B436E4B555E206248D5D93072C85BB2336ADB80A9E75B4AF30D41793C5252005` and `6A9AF7A54C42BD173A483A1C45A6681338CD9AF521F76C8EA4F8B0CFA8B41DCE`.
- Public/package surfaces: `PROJECT_SPEC.md`, `README.md`, `README.zh-CN.md`, `packages/ty-context/README.md`, `packages/ty-context/assets/README.md`, `packages/ty-context/assets/README.zh-CN.md`, historical `docs/launch/github-release-0.8.17.md`, current `docs/launch/github-release-0.8.18.md`, `docs/test-suite-roi-redesign.md` and `docs/symbolic-denotation-efficiency.md`.
- Compact-source surfaces: `.work_products/symbolic-denotation-efficiency/delivery-contract.yaml` and the compact carrier embedded in `docs/symbolic-denotation-efficiency.md`. The final compact carrier contains 113 Facts, 113 obligations and 132 inputs; its Contract `semantic_fact_manifest.sha256` is `74f7a38f74f154764bc9f56727bcc389b876e90bdc948a634b098c34f2694cd2`. `examples/delivery-benchmark/mechanism/admission-set.json` remains a separately frozen admission owner and did not change.
- Recovery surfaces: this index and `.work_products/dra-production-handoff-closure/EXECUTION_SOURCE.md`. The 2360-line SHA-256 `6F8D27533FE86CF7503CC81EAFF7DD7F57A1396E3C1F2FE06ADC364527E734B1` identity is the pre-S6 source; the current complete source identity is the 3412-line SHA-256 `A41493F469EF284E223A57C189494463925BF8CE227A63AB8F54388D42F66C34` recorded above.

## Context Delta

The exact durable Context owners changed by this closure are:

- `project_context/areas/harness-package/contracts/design-resource-authoring.md`
- `project_context/areas/harness-package/contracts/design-resource-handoff.md`
- `project_context/areas/harness-package/contracts/workflow-contract.md`
- `project_context/areas/harness-package/implementation-index.md`
- `project_context/areas/harness-package/verification.md`

No other file is asserted as a Context owner for this amendment.

## Verification record

All results below belong to the same frozen tracked candidate except this recovery-only index update. The candidate build fingerprint is source input SHA-256 `1d7d07bd8ade11d34591c4a928853a1576a5a7e61b1fc1d17298f224f0efbe95` over 421 files and `dist` SHA-256 `188b08a081815fb0db3597a5c2d1a6ba0f87d7886054b5f8e45663fd894dd833` over 830 files.

### Complete package regression

- Final full affected/release-regression run r10 used no cache and no reduced suite. It set only the explicit Long-Task file-isolation switch and ran `npm run test:affected` from the worktree.
- Default lane: status `passed`; 66 files; 411 selected tests; 410 passed; one expected skip; zero failed/cancelled/missing; wall time 207077 ms; critical sentinels 3/3.
- Long-Task lane: status `passed`; 85 files; 585 tests; all 585 passed; zero failed/skipped/cancelled/missing; wall time 3568948 ms; critical sentinels 29/29; maximum one selected file per Node process.
- The log has 86 summary blocks and zero failure lines. Local ignored forensic files are `.artifacts/test-suite/affected-serial-r10.log` (445789 bytes, SHA-256 `083D0A744DCC03341CF97B3857C8D2B8CB8FCC7070EFC1A524CAC9CE2B70C63F`) and `.artifacts/test-suite/affected-serial-r10.exit` (value `0`, SHA-256 `9A271F2A916B0B6EE6CECB2426F0B3206EF074578BE55D9BC94F6F3FE3AB86AA`). The exact invocation is retained locally as `.artifacts/test-suite/run-affected-serial-r10.ps1`.
- The delivery compiler regression file passed 22/22 inside r10, including componentless blocker-only `blocked_external`, planned owner containment, stale blocker, decision blocker and target-drift cases.

### Preserved Windows transient history

- Earlier complete serial attempts r4/r5 observed forged-evidence CLI processes exiting without stdout/stderr; r6 observed a compact-carrier transient; r7 observed a package-promotion worker exit; and r8 observed a Final-Gate mutation worker exit. Their exact isolated cases passed.
- After adding process creation-identity observation, the targeted process soak passed forged evidence 1/1 in 155740 ms, Final-Gate mutation 1/1 in 94404 ms and level-4 package promotion 6/6 in 260224 ms.
- r9 preserved one mechanical failure: `long-task-verification-preview.test.mjs` invoked `git diff --binary --no-ext-diff`, which exited 1 with empty stdout and only LF-to-CRLF warnings on stderr. The rest of that run passed 528/529 Long-Task tests, all 29 critical sentinels passed, and nine later files were not selected because fail-fast stopped the lane. The preserved ignored log is `.artifacts/test-suite/affected-serial-r9.log` (430150 bytes, SHA-256 `B21F4D43E75CE6ED01A3FA793A50DD370090DF2378F890B156D3651B690381E9`).
- The r9 case then passed standalone 2/2 and in five consecutive complete file reruns. The actual-workspace fingerprint probe ran the same Git operation 200 times with zero failures (`core.autocrlf=true`, `safecrlf` unset). The unchanged candidate subsequently passed the complete r10 run, including that file 2/2. Therefore r9 is retained as a non-reproducible Windows mechanical transient, not erased or counted as candidate acceptance.

### Final command and package checks

- Passed: `npm run format:check`, package workspace TypeScript typecheck, package workspace build, `npm run release:check-version`, `make validate-harness`, `ty-context package check-source`, three-copy DRA parity, `npm run verify:active-source-portability`, `npm run test:structural-cost`, and `npm run test:affected:list`.
- `validate-harness` passed Minimal Context and audited 30 touched modules with zero modularity warnings and zero waivers. Active-source portability passed 99 files with zero violations. Structural-cost status was `passed` against baseline SHA-256 `8906dc5e5c3fa02a79002c55dee04e10237c55ba74b4d53ca6f9f016e09af6d5`.
- Affected routing selected `mode=full-suite`, `tier=release-regression`, `requires_build=true`, which is satisfied by r10 rather than by a reduced list.
- Source preview packing produced `project-tiny-context-harness-0.8.17.tgz`, 821512 bytes, SHA-256 `1EAC85F8D1B82B1D4F93D60E5C94F2F2FE79297212A844E3757E63BDC443C67B`. Quickstart smoke passed. Offline launch readiness passed. Tarball smoke passed portable checks plus Long-Task delivery-v2 compile and Live Final Gate acceptance.
- A separate new repository under the system temporary root installed that tarball, initialized Git/npm, ran `ty-context init`, ran an idempotent `sync` (`changed=0`, `skipped=23`, `blocked=0`), validated Minimal Context, and found all nine required generated surfaces. Its `project_context/context.toml` SHA-256 was `279573F205314CC16D1EBAADC5229EA933634294F78192F7BEF188386872CB33`; the temporary repository was then moved to the Windows Recycle Bin and remains recoverable there until the bin is emptied.

## Deliberately not verified

These external/product outcomes remain outside this code-completion theorem and must not be upgraded into claims:

- live Open Design visual-generation quality;
- actual multi-route visual diagnostic results;
- real-product adoption effect.

No npm publish, Git tag, GitHub Release or merge into `main` is part of this Goal. The delivery branch is `codex/scratch-20260824`; its parent baseline is `749aef74525368f63f45dbb24890b2547131c0b5`.

This amendment is executed by the native Goal under the default Workflow Contract. It must not bootstrap `long-task-workflow`; Long-Task is only an affected interoperability and verification surface. The Source file, Recovery Index and native Goal are recovery aids, not Design/Technical Authority, Contract, Gate, state, readiness or acceptance evidence.

## Post-merge S7 main counterexample and isolated closure

- PR #45 merged the exact checked head `e1c96b30fe85a21b9588ff5acf4720a9f3778736` into remote `main` as `dec1e52af8d230e702570bc5ddc69be9c1bc4b5b`. Its merge tree `2f461e468244e2c1b0706794973f72e41ce2c567` is byte-identical to the candidate tree and its parents are the prior main `e0e28e73827251b3562981fcb26f5ecf5e5d0d57` followed by the candidate. There was no merge conflict or conflict-side selection.
- The exact-head PR checks all passed before merge: Harness; both Ubuntu pull-request jobs; both Windows affected launchers; and both Windows Level-4 jobs. The downloaded Ubuntu timing evidence used `github-ubuntu-v3`, no result cache, and passed Trust at 445707 ms and 552232 ms against the 630000-ms budget. Each required 30 applicable Sentinels; `windows-job-pre-resume-containment` was the sole explicit non-applicable Sentinel on Linux. The two Windows Level-4 jobs passed in 8m45s and 9m21s.
- The merge-triggered Package CI run `32811963544` exposed a new main-only Long-Task counterexample in job `97692861183`; the PR Trust lane does not select `long-task-direct-process-observer.test.mjs`. Default passed at 116859 ms. Long-Task remained within its 1200000-ms budget at 701312 ms, but fail-fast stopped after `direct process observation captures one root stdout envelope and host attestation` failed; 277 tests passed, one failed, eleven were expected platform skips and 46 files were consequently missing. Both `short-descendant` and `short-grandchild` roots completed in about 43 ms while their deliberately detached descendants remained alive.
- S7 changed neither that test nor the POSIX process observer/product sources. The counterexample is a fixture-topology mismatch: Windows pre-resume Job Object containment is authoritative even when every descendant requests a detached topology, while the retained POSIX fallback combines a root process group with sampled `ps` discovery for descendants that deliberately leave the group. An immediate root plus a child that calls `setsid` can disappear before the first sampled parent/child relation and cannot truthfully be upgraded into pre-resume Job semantics.
- The isolated fixture repair keeps the Windows case exactly adversarial (`detached=true` for both child and grandchild), while POSIX short-root descendants inherit the already-created root process group (`detached=false`). The fixture records and asserts the selected topology for every spawned level. The root still exits immediately, the descendant still lives for 60 seconds, the same non-completed/error/all-processes-gone assertions remain, and the separate POSIX long-root detached-child case continues to prove sampled escaped-descendant detection. The topology repair itself changes no product supervisor, Authority, Binding, Claim, Gate, Registry, state, timeout or package bytes.
- Because the merge incident also proved that PR Trust omitted this real observer lifecycle file, the final follow-up additionally registers `[critical:direct-process-descendant-containment]` in the existing `CRITICAL_TEST_SENTINELS` owner and raises the reviewed Trust maximum from 26 to 27 files. It adds no workflow, CI job, registry, suite, lane or acceptance authority: existing `test:trust:built` now executes the changed three-case file on Ubuntu and Windows before merge, under the unchanged `github-ubuntu-v3` 630000-ms Trust budget. Its rationale is deliberately bounded to real root/descendant containment and cleanup on the existing Windows Job and POSIX process-group/sampled-escape topologies; it does not claim hostile-host isolation or Windows-equivalent POSIX pre-resume containment.
- Merge-triggered OpenSSF Scorecard run `32811963555` separately completed its analysis but failed external publication because scorecard.dev returned HTTP 400 and classified the repository's existing pinned action SHA `99c09fe975337306107572b4fdf4db224cf8e2f2` as an imposter commit. PR #45 did not change `.github/workflows/scorecard.yml`; this external security-workflow maintenance item is preserved for audit and is not folded into the POSIX fixture repair.
- The follow-up branch is `codex/dra-ci-main-observer-closure`, created only in `E:/Dev/worktrees/project-tiny-context-harness/dra-ci-evidence-closure` from `origin/main@dec1e52a`. The original local `main` checkout remains read-only and must not be switched, staged, reset, committed or otherwise written. Acceptance requires focused Windows and Linux evidence, affected/full required suites without cache or population reduction, an exact-head follow-up PR, and a green merge-triggered main Package CI before this native Goal can complete.
- Before freezing the repair, the exact Windows direct-observer file passed once and then passed ten consecutive soak iterations: 30/30 tests, with the Windows short child and grandchild topology asserted as detached on every run. The worktree-aware affected plan selected five files and required a build; its fresh run passed 32/32 tests. The build fingerprint remained input SHA-256 `924b33e199f1d090089f57a4766fc50c33601850cf38613bf8735cbfae1f8cfe` and dist SHA-256 `5bc7ceda2218e5be88c39ab8211c04cce496ec6cbe856c63b272c2d4cd5b1c19`, confirming that the package product bytes did not change. These are pre-freeze Windows/localization checks; Ubuntu CI remains the authority for the POSIX branch and no pre-freeze result authorizes merge.
- After the Trust registration change, the first two-file owner run passed the three direct-observer cases but the existing policy-continuity test correctly stopped on its stale fixed total (`expected 34`, `actual 35`). That deterministic synchronization omission was updated to 35 and augmented with an explicit assertion that `long-task-direct-process-observer.test.mjs` is in `LONG_TASK_TRUST_TEST_FILES`; the same two-file command then passed 17/17 with both `direct-process-descendant-containment` and `critical-policy-continuity` observed. `make validate-context`, `npm run format:check` and `git diff --check` also passed on that pre-freeze candidate. This preserved failure is not counted as acceptance evidence for the later committed head.
- One pre-freeze short-gate batch incorrectly ran package `build` concurrently with `make validate-harness` and `test:structural-cost`. While build was replacing `dist`, those read-only checks respectively observed transient missing `design-resource-reconciliation.js` and `context-graph-snapshot.js` imports. The concurrent build completed successfully with the unchanged package fingerprint; this is an invocation-order error, not a candidate result. Both readers must pass after build on a static `dist`, and no final aggregate or gate may run concurrently with build.
- After build completed, `make validate-harness` passed Minimal Context and audited the three touched executable test/policy modules with zero warnings and zero waivers; `test:structural-cost` also passed. The worktree-aware affected plan selected 19 files and required the current build fingerprint; `npm run test:affected -- --no-build` then passed 181/181 tests with zero failure/skip/cancel. Package source parity, release-version parity, active-source portability (100 files, zero violations) and `git diff --check` passed. The build fingerprint remained input SHA-256 `924b33e199f1d090089f57a4766fc50c33601850cf38613bf8735cbfae1f8cfe` and dist SHA-256 `5bc7ceda2218e5be88c39ab8211c04cce496ec6cbe856c63b272c2d4cd5b1c19`.
