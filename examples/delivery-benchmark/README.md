# Historical benchmark results

The schema-4 workflow benchmark runner, proof fixtures and admission mechanisms are retired. Preserved files under results/ record earlier versions only; they do not describe current capabilities or prove schema-5 performance. Current package tests and tarball consumers live in tests/ty-context.
